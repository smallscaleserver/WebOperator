# WebOperator — Project Plan

Actionable version of the roadmap in `README.md`. Update the checkboxes as
work lands — this file (together with `AGENTS.md`) is the canonical place to
see current status, regardless of which tool (Claude Code, Codex CLI, or a
human) is picking the work back up.

## Decision Log

| Decision | Choice | Why |
| --- | --- | --- |
| Project name | **WebOperator** (single name, repo + product) | Repo was already public under this name; earlier "WebSteward" codename retired to avoid confusion. |
| Core stack | TypeScript/Node.js + Playwright | Best Chromium/Firefox automation ecosystem; persistent profiles, trace/video, headed mode all supported natively. |
| Browsers | Chrome **and** Firefox from Phase 1 | User requirement: must feel like a real PC, not a single-browser bot. |
| Display/remote control | Xvfb + Fluxbox + x11vnc + noVNC | Gives a real, clickable desktop reachable from any browser tab — no native VNC client needed. |
| Sandbox | Chromium runs with `--no-sandbox` for now | Simplest path to a working Phase 1. Real seccomp/user-namespace hardening per Playwright's official Docker guide is deferred to Phase 5. |
| Session storage | Plain bind-mounted volume (`data/profiles/*`) for now | Placeholder for the future encrypted Session Vault (Phase 2/3). Not safe for real credentials yet — dev-only. |
| Worker transport | `playwright-core` + `connectOverCDP`, worker container joins `browser-worker-chrome`'s network namespace (`network_mode: service:...`) | Chromium ignores `--remote-debugging-address` for a headed instance and only ever binds CDP to `127.0.0.1` — sharing the network namespace reaches it without publishing the (unauthenticated) CDP port anywhere. Firefox has no CDP equivalent; needs WebDriver BiDi later. |
| Session proof method | Synthetic marker (cookie + localStorage on `example.com`), not a real login | No site adapter exists yet (separate checklist item). Proves the `storageState` save/restore mechanism generically; a real login-based proof lands with the first adapter. |
| Session file storage | Plain JSON under `data/sessions/*` for now | Same caveat already logged for `data/profiles/*` — placeholder for the encrypted Session Vault (Phase 2/3), not safe for real credentials yet. |
| Demo adapter target | `https://the-internet.herokuapp.com` (`/login`, `/entry_ad`) | Free, purpose-built practice app that exists specifically to be automated against; publishes its own test credentials. Sidesteps any "is it okay to automate this real site" question while still exercising a real login, a real session cookie, and a real popup-dismissal case. |
| Control Panel deployment | Runs as a plain host Node process (`services/control-panel`, `npm start`), not a Docker service | Its whole job is running `docker compose` commands. Containerizing it would mean mounting the Docker socket (Docker-out-of-Docker) just to shell back to the same Docker Desktop already on the host — real security surface (socket access ≈ root on host) for no benefit at this stage. Revisit if/when this needs to run somewhere without a host Docker CLI. |
| Redis | `redis:7-alpine` compose service, bound to `127.0.0.1:6379` only, no volume | No auth on Redis by default, so loopback-only — same posture as CDP/Control Panel. Ephemeral (no persistence) is fine; job history doesn't need to survive `docker compose down` at this stage. |
| Queue worker location | ~~BullMQ producer *and* consumer both run inside the Control Panel process~~ **Superseded**: split into two processes (`npm start` = API/UI producer, `npm run worker` = consumer) within the same `services/control-panel` project — see the later "queue consumer split" row below. |
| Queue concurrency | 1 | All 4 queueable actions connect to the same shared `browser-worker-chrome` over CDP — the queue's job is to serialize access to that one browser, not parallelize it. Verified via BullMQ `processedOn` timestamps: job N+1's `processedOn` exactly equals job N's `finishedOn`. |
| Step reporting | Post-hoc, parsed out of captured stdout (`WEBOP_STEP {...}` lines), not live-streamed | True live progress needs `spawn` + incremental parsing + BullMQ `job.updateProgress()` — real complexity for a slice that's mainly about debugging after the fact, which post-hoc parsing already delivers. Live streaming is a reasonable follow-up, not required now. |
| Step/screenshot scope | Step name + ok/error + optional detail message + optional screenshot filename + timestamp. No DOM snapshot, console/network log capture, or full Playwright `.trace.zip` yet | README's Event Recovery Engine table wants much more (DOM snapshot, console/network errors); kept to the smallest slice that makes job failures debuggable without reading raw logs. Fuller trace capture stays open. |
| Workflow definition format | Named JSON files checked into `services/worker/workflows/*.json`, not request-supplied JSON | Auditable (it's source, reviewed like any other code), and the Control Panel only ever needs to validate a *name* against real files on disk before enqueueing — no new arbitrary-JSON-from-a-web-request input-validation surface to design. |
| Workflow engine vs fixed actions | Added *alongside* the existing 4 fixed actions/`adapters/the-internet.ts`, not a replacement | Those were already proven working across several sessions; no reason to destabilize them to add the new generic path. Migrating the old path onto the new engine is a deliberate follow-up, not bundled into this change. |
| Firefox automation mechanism | Playwright's own `launchServer()`/`connect()` protocol, **not** literally WebDriver BiDi | Empirically tested before building: Playwright has no public API for connecting to an *externally-launched* Firefox the way `connectOverCDP` works for Chromium. It needs its own patched Firefox build (`npx playwright install firefox`, not apt `firefox-esr`) and a `launchServer()` call in the browser-worker-firefox container + a separate `connect()` call from `worker-firefox` — this does work, tested directly. The original checklist wording ("BiDi") was an unverified assumption; corrected here rather than left wrong. |
| Firefox profile persistence | Dropped | `launchServer()` explicitly rejects a `-profile` argument ("Pass userDataDir to launchPersistentContext instead"), and `launchPersistentContext` has no server/connect equivalent — Playwright forces a choice between "persistent profile, single process" and "separate connecting process," and our architecture (long-lived browser container + short-lived worker containers) needs the latter. `storageState` remains the real session-continuity mechanism (already proven for Chromium); the `data/profiles/firefox` bind mount is now unused (left in place, harmless, cheaper than restructuring further). |
| Firefox page/context lifecycle | Tied to the connecting client, unlike Chromium | Discovered while verifying: after `worker-firefox`'s script disconnects, the page/context it created disappears from the noVNC view (confirmed via process count + a direct before/after check), even though the Firefox *server* process itself survives (same persistent-container model as Chrome). So a human watching noVNC only sees Firefox actually doing something *while* a job is running, not before or after — a real, documented difference from Chromium's persistent default page. Not fixed now; noted as a real limitation. |
| Firefox automation scope | One demo script (`run-firefox-demo.ts`) proving the connection mechanism, not full adapter/workflow parity | Matches how Chromium's own CDP support was scoped in its first session — prove the mechanism, extend later. |
| Workflow validation depth | Full structural validation (every step's `type` known, `params` shape) happens in `run-workflow.ts` *before connecting to any browser*; the Control Panel only does a cheap JSON/shape sanity check at enqueue time, not a full type-registry check | The registry (`ACTION_HANDLERS`) lives in the separate `services/worker` npm project — duplicating it into `services/control-panel` would just be a second list to keep in sync for no real safety gain, since the worker-side check already guarantees zero partial execution (it runs first, before any step, before any browser interaction). |
| Job timing fields | Added `processedOn`/`durationMs` to the job API (from BullMQ's own `job.processedOn`/`job.finishedOn`, already tracked internally, just not previously surfaced) | Cheap, no new instrumentation needed — the data already existed inside BullMQ. |
| Per-step retry defaults | Only `navigate`/`dismissPopup`/`extract`/`screenshot` retry by default (2 attempts, 1s fixed delay); `login`/`saveSession` get zero implicit retry, only if a workflow step explicitly sets `retry` | The first four are read-only or idempotent — safe to retry blindly. `login`/`saveSession` are state-changing; blind retry risks double-submitting a form or writing a session file mid-failure. Matches the explicit ask: retry the safe stuff by default, require an explicit opt-in for anything state-changing. |
| Per-step retry backoff | Fixed delay, no cap on `attempts` beyond "must be positive" | Consistent with the existing job-level retry policy (also fixed delay). Workflow JSON is trusted source (reviewed like any other code), not untrusted input, so an artificial attempts cap isn't adding real safety — the existing 120s exec timeout is already the outer bound. |
| Attempt info visibility | Only shown when it's actually informative: on success, only if it took more than one attempt; always shown once all attempts are exhausted on failure | First implementation showed `(attempt 1/2)` on *every* successful default-retryable step, even ones that succeeded immediately — pure noise. Caught by testing the real workflow end-to-end and comparing before/after, not assumed; fixed same-session. |
| Queue consumer split | Split into two processes within `services/control-panel`: `src/server.ts` (`npm start`) is now API/UI + producer only; new `src/worker.ts` (`npm run worker`) is the consumer only. Not a separate top-level service/project. | Restarting the panel used to also kill whatever job was mid-flight, since one process owned both roles. Verified both directions directly: killed the API process while a job was actively running on the worker — job still completed; killed the worker with the API up — the next enqueued job correctly sat in `waiting` (not lost) until the worker came back and picked it up. |
| Redundant Queue connection in the worker process | Accepted, not fixed | `queue.ts`'s module-level `Queue` producer object is created unconditionally at import time regardless of which entry point imports it, so the worker process ends up holding an unused producer connection to Redis alongside its consumer connection. Splitting `queue.ts` further into producer-only/consumer-only modules would remove this, but it's a harmless extra Redis client, not worth the extra file churn for a dev tool. |
| Windows orphan-process gotcha, take two | Confirmed the same issue documented for the Control Panel API process also affects the new worker process | The worker holds no port, so the `Get-NetTCPConnection -LocalPort 4000` check used to detect the API's orphaned process doesn't apply — verify via `Get-CimInstance Win32_Process | Where CommandLine -like '*worker.ts*'` instead. Hit and had to work around this again while verifying this very change. |
| MinIO/S3 artifact storage | Added *alongside* the existing local `data/worker-output/*` files, not replacing them | Same additive-not-destabilizing pattern as the workflow engine. The Control Panel's `/screenshots/*` route keeps serving local files unchanged — MinIO is a second copy, not the source of truth (yet). |
| Artifact upload failure handling | New `stepBestEffort()` primitive in `services/worker/src/steps.ts`: reports ok/error like any other step (visible in the Jobs UI) but never rethrows | A MinIO hiccup must not turn an otherwise-successful login/extract job into a failure — archival is observability-grade, not load-bearing. Verified directly: stopped the `minio` container and re-ran a job — `archive-screenshot` reported `status: "error"` (`getaddrinfo ENOTFOUND minio`) while the job still exited 0 and the screenshot itself still saved locally. |
| Worker→MinIO connectivity | Docker's embedded service DNS (`MINIO_ENDPOINT=minio`), reached through `worker`'s inherited network namespace (`network_mode: service:browser-worker-chrome`, which is itself attached to the default `weboperator_default` bridge network) | Confirmed empirically, not assumed — this was the first time a worker container needed to reach a *sibling* compose service by name rather than `localhost` (CDP/Firefox-launchServer are processes inside the browser container itself). Round-trip proof: uploaded a screenshot, fetched it back from `127.0.0.1:9000` with a throwaway script, compared SHA-256 hashes — byte-identical. |
| MinIO/S3 scope | Screenshots (session 3) *and now session files* (session 4), both uploaded to the same `weboperator-artifacts` bucket under separate prefixes: `screenshots/<filename>` and `sessions/<filename>` | Matches the explicit instruction to start from screenshot artifacts, then extend to session files as the next slice. Wiring MinIO into the Control Panel UI/API (still reads local files only) remains a separate, natural follow-up. |
| Session-file archival storage posture | Plain JSON in MinIO, same as `data/sessions/*` locally — not an encryption upgrade | A second storage location for the same dev-only plaintext data, ahead of the encrypted Session Vault (Phase 2/3). Reuses `uploadArtifact()`/`stepBestEffort()` unchanged from the screenshot work — same `sessions/` prefix in the same bucket, same non-fatal-on-failure behavior, no new code in `artifacts.ts`/`steps.ts`. |
| Session content exposure | No Control-Panel-side change needed to keep session content out of the UI/logs | Confirmed via code search: the Control Panel has no session-related routes or UI at all today, so there was nothing to redact. The `archive-session` step reports only name/status/timestamp/error (via `stepBestEffort`, which never passes `captureResult`) — the same shape as `archive-screenshot` — so cookie/token values never enter a `WEBOP_STEP` line or the Jobs API by construction. |
| Control Panel → MinIO artifact retrieval | New `GET /api/artifacts/screenshots/:filename` route (`services/control-panel/src/server.ts` + new `src/artifacts.ts`) streams the object directly from MinIO through the Express server, rather than issuing a browser-facing presigned-URL redirect | A server-side stream means every failure mode (MinIO down, object missing, bad filename) is caught in one place and returned as a clear JSON error with an appropriate status code — a redirect to a presigned URL would instead surface a raw, unhandled browser network error with no chance to normalize it. Filename validated against a strict allowlist regex before it ever reaches MinIO, same defensive posture as the existing fixed action allowlist in `actions.ts`. |
| MinIO error message quality | `err.message` is empty for a Node `AggregateError` on connection refusal (`ECONNREFUSED` lives in `.code`, not `.message`); a missing object is a distinct `S3Error` with `.code === "NoSuchKey"`, also not reflected in `.message` | Caught by testing both failure modes directly rather than assumed: the first version of the route returned `"MinIO unavailable: "` (blank) when MinIO was stopped, and classified every not-found as a generic 502 instead of 404. Fixed to fall back to `.code` for the message and to branch on `.code === "NoSuchKey"` specifically. |
| MinIO link visibility in the job step UI | The new "MinIO" link next to a screenshot step only renders when that step's matching `archive-*` step reports `status: "ok"` | Avoids a dead link when archival failed or MinIO was down for that particular job — the UI already shows the archive step's own ✅/❌, so the link's presence stays consistent with what's actually retrievable. |
| Session-reading route | Not added | Out of scope by the explicit instruction, and there was nothing to build to begin with — no session UI exists to extend, so "never expose session content" is satisfied by not adding any new surface, same reasoning as the prior session's archival work. |
| Download artifacts — real blocker found | **Deferred**, not implemented. `download.saveAs()` and `download.createReadStream()` both fail to retrieve real bytes when Playwright connects via `connectOverCDP` to Chromium launched as a raw external process (`entrypoint.sh`'s `chromium ...`) in a *separate container* from the worker driving it. Confirmed empirically, twice: once on the pre-existing shared default page, once again on a context Playwright created itself (`browser.newContext()`) — same failure both times, so this is not a shared-context issue. `saveAs()` throws `ENOENT` copying from a local temp path that was never actually populated; `createReadStream()` resolves without error but the destination file is 0 bytes. Root cause: Chromium and the worker run in different containers with different filesystems (only the network namespace is shared, via `network_mode: service:browser-worker-chrome`) — Playwright's download-artifact retrieval assumes local filesystem access to wherever the browser wrote the file, which doesn't hold here. This is a known rough edge of `connectOverCDP` against externally-launched browsers, not a bug in the worker's own code. |
| Download artifacts — the real fix, not built this round | Would need: (1) a new Docker volume shared between `browser-worker-chrome` and `worker` (they currently share network, not volumes); (2) a raw CDP session (`page.context().newCDPSession(page)` → `Page.setDownloadBehavior({behavior: "allow", downloadPath: ...})`) to force Chromium to write completed downloads into that shared path, since Playwright's own download API can't retrieve them; (3) worker-side logic to detect the completed file (Playwright's `download` event/object can't be trusted for the file itself here, only as a "a download started" signal) and copy/move it into `OUTPUT_DIR` | Explicitly not built this round — user's own call after seeing the empirical finding: a new shared volume + raw CDP + file-watching is materially bigger and more invasive than what was scoped ("artifact abstraction supporting multiple types," not full production coverage of every type), and introduces new failure modes (file-watch races, cleanup of the shared directory) that deserve deliberate design together with video/trace and artifact lifecycle/cleanup, not a rushed bolt-on. |
| Video/trace artifacts | Not implemented, design note only | Both Playwright video recording (`recordVideo` on `browser.newContext()`) and tracing (`context.tracing.start()`/`stop()`) are **context-creation-time** options — unlike `download` (which, blocker aside, could reuse the existing shared page), these can't be retrofitted onto the already-running shared default context at all. Would need a dedicated context per job/workflow, a bigger architectural change with no concrete workflow asking for it yet. Matches the user's explicit instruction not to force Playwright tracing/video into scope without a clear use case. |
| Artifact read route generalized by kind | `GET /api/artifacts/screenshots/:filename` (single-kind, from the prior round) became `GET /api/artifacts/:kind/:filename` with an explicit `READABLE_ARTIFACT_KINDS` allowlist (`screenshots`, `downloads`, `videos`, `traces` — **`sessions` deliberately never included**) | Delivers the actual ask for this round — "artifact abstraction supporting multiple types" — as a real, generic, working route, even though only `screenshots` has real content today. Reads for `downloads`/`videos`/`traces` correctly 404 ("not found") rather than 400, since the kind itself is valid, just empty — the route won't need another shape change once a real producer exists for any of them. `sessions` returns 400 "unknown or unreadable kind" — this is the requirement enforced structurally by an allowlist, not by mere absence of a route (which was the prior round's weaker guarantee). Verified: existing `/api/artifacts/screenshots/*` URLs still work unchanged (same URL shape), local `/screenshots/*` unaffected, `sessions`/unknown kinds 400, `downloads`/`videos`/`traces` 404 (no producer yet), MinIO-down failure handling (`ECONNREFUSED` fallback, panel stays alive) carries over unchanged from the prior round. |
| Fixed actions → workflow engine, "demo" and "adapter" | `runStart`/`runAdapter` retired from `services/control-panel/src/actions.ts`'s `ACTIONS` table and `queue.ts`'s `QUEUEABLE_ACTIONS`; their Control Panel buttons now enqueue workflows (`demo` — new file, `services/worker/workflows/demo.json` — and the already-existing `the-internet-login`) instead of a fixed `docker compose run` invocation | Both map cleanly onto the existing generic action registry (`navigate`+`screenshot` for demo; `dismissPopup`/`login`/`extract`/`saveSession`/`screenshot` for adapter, which is exactly what `the-internet-login.json` already did — no new workflow file needed for it, just repointing the button). Underlying `services/worker` npm scripts (`npm run start`/`npm run adapter`) are untouched and still directly runnable via `docker compose run --rm worker npm run <script>`, per the explicit "keep as dev/debug CLI" allowance — only the Control Panel's enqueueable-action registration was removed. Verified: `POST /api/enqueue/runStart`/`runAdapter` now cleanly 400 "not a queueable action" (no dangling half-registered path); both buttons' workflows run correctly end to end through the real queue with archival/MinIO links intact. |
| Fixed actions → workflow engine, "save" and "restore" — not migrated | `runSave`/`runRestore` (`save-session.ts`/`restore-session.ts`) intentionally left as fixed actions, unchanged | Neither maps onto the current generic action registry without adding new action types, which the explicit "to the extent generic actions support it" instruction ruled out this round. `save` sets a synthetic cookie+localStorage marker via bespoke inline JS (`context.addCookies`, `page.evaluate`) — no generic "set a cookie"/"eval JS" action exists. `restore` creates a **fresh, isolated** `browser.newContext({ storageState: ... })` and reads cookies/localStorage back directly — `run-workflow.ts`'s model is one shared `{page, context}` established once at the top of a run; there's no action type for swapping the working context mid-workflow, and designing one (does a workflow ever need multiple contexts? how would JSON express that?) is a bigger question than this round's scope. Documented rather than forced. |
| Stale `worker-firefox` Docker image | Found and fixed while regression-checking the Firefox demo button (unrelated to this round's actual change): the image hadn't been rebuilt since `minio` was added to `services/worker/package.json` in an earlier session, so it failed with `ERR_MODULE_NOT_FOUND: minio`. Rebuilt via `docker compose build worker-firefox` | `services/worker`'s own `worker` image had already been rebuilt when the `minio` dependency first landed, but `worker-firefox` — a separate image tag built from the same Dockerfile/service directory — was missed at the time. Caught by actually running the regression check rather than assuming it still worked; same "verify empirically" discipline as everything else in this project. |
| Gmail OAuth/API scaffold, round one | `services/worker/src/gmail/{client,authorize,list-messages}.ts` — a dev/local scaffold only, added alongside everything else, nothing removed or replaced | Explicit instruction: OAuth/Gmail API scaffold first, no browser automation of the Gmail login page, no plaintext secrets, not the real encrypted vault yet. Matches `README.md`'s own stated Gmail architecture (API primary, browser fallback only when necessary, least-privilege scopes) — this is the first real slice of that existing plan. |
| Gmail token storage | Plain JSON file at `data/gmail-tokens/gmail-token.json` (path overridable via `GMAIL_TOKEN_PATH`), inside the repo's already-blanket-gitignored `data/` — verified genuinely ignored via `git check-ignore -v`, not just assumed | Same posture already documented and accepted for `data/profiles/*` and `data/sessions/*`: plaintext, unencrypted, dev-only, explicitly not safe for a real account. A real encrypted vault is its own separate, later Phase 3 checklist item — pretending this round's placeholder is safe would be worse than being plain that it isn't. |
| Gmail OAuth scope | `https://www.googleapis.com/auth/gmail.readonly` only, `access_type: "offline"` + `prompt: "consent"` on the auth URL | Least-privilege per `README.md`'s own OAuth principle — no send/modify scope until a feature actually needs one. `prompt: "consent"` is required to reliably get a `refresh_token` back on every authorization, not just the first one ever granted. |
| Gmail scripts run on the host, not in the worker Docker container | `npm run gmail:authorize`/`npm run gmail:list` run via `cd services/worker && npx tsx ...` directly, unlike every other worker script (`docker compose run --rm worker ...`) | These don't touch Playwright/CDP/the browser container at all — pure Node HTTPS calls to Google. The OAuth redirect must land on a `localhost` port the user's own host browser can reach directly; a container can't expose that without extra port-publishing for zero benefit. A real, architectural reason for the deviation, not an inconsistency. |
| Gmail OAuth callback listener design | `authorize.ts` starts a plain Node `http` server bound to the exact host/port/path parsed out of `GOOGLE_REDIRECT_URI` itself (not a separately hardcoded port), handles exactly one redirect, then closes | Standard CLI-based Google OAuth pattern (same idea `gcloud auth login` uses). Parsing the port from the configured redirect URI instead of hardcoding a second value elsewhere means the Google Cloud Console config and what the script actually listens on can never drift apart. Verified for real: started the script with fake-but-well-formed credentials, curled the callback URL with a fake `code`, confirmed the listener correctly parsed it, responded to the "browser," and attempted the real token exchange with Google — which Google correctly rejected (`invalid_client`, since the credentials were fake) and the script's error handling caught cleanly (exit code 1, readable message, not a stack trace), then the listener port was confirmed closed. This is not a live account/data test — no real Google Cloud OAuth client exists in this environment, and one wasn't set up, per the explicit instruction to ask before any live test. |
| Gmail read proof scope | `list-messages.ts` prints only message count + IDs, never subjects/snippets/bodies | Minimal existence proof, not a mail reader — avoids dumping potentially sensitive content to stdout/logs by default for a feature whose whole point right now is just proving the auth plumbing works. |
| Control Panel Gmail surface | Static informational `<section>` only (env vars/CLI commands, dev-only token caveat) — no new JS, no new API route, no button | Explicit instruction: a full OAuth UI (spawning/tracking the local listener process from the Panel, surfacing the consent URL, knowing when the callback lands) is a real feature with its own design questions, correctly out of scope for this round. |
| README testing guide, feature work paused | Explicit instruction to stop feature work for a round and document how to actually test everything shipped through Phase 2 — replaced the stale "Phase 1 quickstart" (predated the Control Panel/MinIO/workflow engine entirely) with a full step-by-step guide + troubleshooting section in `README.md`, ran every step for real rather than just checking command syntax | No feature work landed this round by design — this is a checklist/docs-only note, not a Phase 3 status change. See `docs/AGENT_HANDOFF.md` session 9 for the explicit "don't start new feature work, including Gmail/Phase 3, until the user has tested and given direction" note for other sessions/tools picking this up. |
| XC Bank isolation architecture | New `services/xc-bank` (Node/Express, in-memory state only, no database) is a genuinely separate service, reached by the worker **only** over HTTP (`http://xc-bank:3000` inside Compose, same service-DNS mechanism already proven for `minio`) — no shared code, no shared Redis/BullMQ/MinIO, no shared `data/` files, no internal API calls. `services/worker/src/adapters/xc-bank.ts` extracts exclusively via Playwright DOM locators, never an internal endpoint | Explicit requirement: XC Bank must be indistinguishable, architecturally, from a real third-party site like `the-internet.herokuapp.com` — the whole point is proving extraction reads live DOM state. Verified structurally, not just by convention: grepped `services/worker/src` and `services/control-panel/src` for any `xc-bank` import path — the only matches are the adapter's own HTTP base-URL string and comments; nothing imports `services/xc-bank` source. |
| XC Bank deterministic data generation | A small self-contained seeded PRNG (mulberry32, no new dependency), seeded from `sessionId + regenerateEpoch + a 10-second time-window bucket` | Balances "provably not hard-coded" against "verifiable by a human without a race": stable within one 10s window (three rapid requests returned identical data, confirmed directly), changes automatically once the window rolls over (confirmed directly — 11s wait produced a different balance), and a dev-only `POST /dev/regenerate` endpoint forces a fresh seed immediately by bumping a per-session epoch, without waiting for the window. |
| XC Bank two new workflow action types | `xcBankLogin`/`xcBankExtractDashboard` added to `services/worker/src/actions/registry.ts`'s registry, wired into `run-workflow.ts` (the latter added to the auto-retry set like `extract`, since it's read-only; the former left out, like `login`, since it's state-changing) — both return a short human-readable string (not the raw object) so the *existing* Jobs UI `String(s.data)` rendering already displays a clean summary with zero `app.js` changes. Full structured detail (`{balance, transactionCount, transactions}`) still goes to `console.log`, inspectable via the job's captured stdout | Reuses the exact `captureResult`/step-reporting mechanism `extract` already established, rather than adding new UI code for one adapter. Verified for real, twice in a row without restarting the browser container: first run reported "Logged in as demo_user (fresh two-step login)"; second run (same shared browser session, cookie still present) reported "Already authenticated as demo_user (session reused, password step skipped)" — both branches of the login logic genuinely exercised, not just read in the code. |
| XC Bank Control Panel integration | No Control Panel code changes at all — the existing dynamic Workflows section (`GET /api/workflows` + `app.js`'s `loadWorkflows()`) already lists every file under `services/worker/workflows/*.json` with a run button | Direct continuation of the fixed-action-migration decision (workflow engine is the primary path) — confirmed `xc-bank-login-extract` appeared in `GET /api/workflows`'s response automatically the moment the JSON file existed, no restart-triggering code change needed. |
| Future Gmail/email notification design (XC Bank) — design-only, not implemented | `Transaction` (`services/xc-bank/src/transactions.ts`) carries `notificationSubjectTemplate` and `notificationStatus` (always `"mock-only"` this round) — fields a *future* round could use to generate an email-shaped payload from a transaction, without a schema change. No mock outbox, no email generation, no SMTP, no Gmail API call, no OAuth exists yet for this — XC Bank does not talk to Gmail/Google APIs directly, ever; that boundary stays with WebOperator's own separate Gmail ingestion (Phase 3, already scaffolded in `services/worker/src/gmail/`, untouched this round). A future implementation would add a mock outbox/event log *inside* XC Bank (still isolated, still HTTP-only from WebOperator's side) that logs notification-shaped records when transactions generate; WebOperator's Gmail ingestion would separately read real Gmail via its own OAuth scaffold and correlate by reference/id, amount, and timestamp — no real email sending, no shared DB, no internal API between the two sides, matching the same isolation rule as everything else in this feature. | Explicit user instruction: reserve the shape now, build nothing email-related this round, don't start Phase 3 work. |
| XC Bank remembered-username login | `GET /login` (`services/xc-bank/src/server.ts`) now checks for a *pending* session (username captured, not yet authenticated) and redirects to `/password`, not just a fully `authenticated` one — previously a pending session was silently re-shown the username form. Session data model (`{ id, username, authenticated, regenerateEpoch }`) needed no change, this was a routing-logic gap, not a data-model gap | Matches real bank UX (most banks remember the username across visits and only ask for password on return). The worker adapter's `login()` now branches on three URL outcomes after navigating to `/login` — `/dashboard` (already authenticated), `/password` (remembered username, fill password only), or still on `/login` (fresh, fill both) — verified directly for all three paths, not assumed from the route logic alone. |
| XC Bank `Logout` vs `Logout clean` | `POST /logout` flips `authenticated` to `false` but **keeps** `username` on the same session — the next `/login` correctly bounces to `/password` (matches the remembered-username behavior above). `POST /logout-clean` deletes the session entirely (`sessions.ts`'s new `deleteSession()`) and clears the cookie — the next `/login` shows a genuinely fresh username form. Both are real DOM buttons (`#logout-btn` on `/dashboard`; `#logout-clean-btn` on both `/dashboard` and `/password`, as an escape hatch from a stuck pending state) | `Logout clean` is explicitly a **dev/test-only reset helper**, not simulating real bank behavior — real banks don't offer a "forget me entirely" self-service button. Documented as such everywhere it's mentioned (`README.md`, `AGENTS.md`) so it's never mistaken for realistic behavior to test against. |
| XC Bank `xcBankLogoutClean` worker action | New action (`services/worker/src/actions/registry.ts`) + one-step workflow `xc-bank-logout-clean.json` — clicks the real `#logout-clean-btn` via Playwright, never a raw request to the route (same DOM-only posture as every other XC Bank action). Idempotent by design (a no-op if already clean), so added to `run-workflow.ts`'s auto-retry set alongside `xcBankExtractDashboard` | Lets a workflow (or a human via the Control Panel) reset XC Bank's test state on demand without touching noVNC. Verified for real through the actual queue: ran `xc-bank-logout-clean`, then `xc-bank-login-extract` immediately after — the login step reported `path: "fresh"` again, proving the reset genuinely took effect end-to-end, not just that the route returned 200. |
| XC Bank Monitor — reuse over rebuild | The monitor's "check" is exactly one run of the *existing* `xcBankLogin` + `xcBankExtractDashboard` actions, unchanged — no new worker-side login/extraction logic | Session-aware reuse (still logged in → skip; redirected → re-login via the already-proven 3-path flow) comes for free by reusing what's already there and already verified, rather than re-implementing it for the monitor specifically. |
| XC Bank Monitor — `${ENV_VAR}` workflow param substitution | `run-workflow.ts`'s `resolveParams()` resolves a string param of the exact form `"${SOME_VAR}"` against `process.env`, right after loading the workflow JSON — an unresolved (unset) var becomes `""`, which trips the *existing* `requireString` validation with a clear error rather than a garbled literal filename | Needed so `xc-bank-monitor-check.json` gets a distinct screenshot filename per run (the existing `xc-bank-login-extract.json` always overwrites the same file, which is correct for *that* workflow's own purpose and stays unchanged) — a small, generically useful addition, not a monitor-specific hack. |
| XC Bank Monitor — structured data out of the worker | `xcBankExtractDashboard` now logs a single-line `XC_BANK_DASHBOARD {...}` marker (was multi-line pretty-printed JSON, not reliably parseable) — same convention as the existing `WEBOP_STEP` lines. `services/control-panel/src/exec.ts`'s new `parseXcBankDashboard()` mirrors the existing `parseSteps()`'s grep-a-prefixed-line approach | Gives the Control Panel real per-transaction structured data (`id`/`direction`/`amount`/`counterparty`/`timestamp`/`balanceAfter`) for dedup, without changing what the regular Jobs UI shows (the short human-readable return string is untouched) or adding a second communication channel — it's the Control Panel reading its own child process's stdout, nothing to do with the XC Bank isolation rule. `ExtractedTransaction` (`services/worker/src/adapters/xc-bank.ts`) gained `timestamp`/`balanceAfter` fields it didn't extract before (the DOM cells already existed, just weren't read) — needed for chronological dedup and the notification display. |
| XC Bank Monitor — scheduling mechanism | A BullMQ **Job Scheduler** (`upsertJobScheduler`/`removeJobScheduler`/`getJobSchedulers`, the current non-deprecated repeatable-jobs API in the installed BullMQ 5.81.3), not a second locking mechanism — the scheduled check runs through the *same* `Worker(..., { concurrency: 1 })` instance as every manual action/workflow job, so it can never run concurrently with (or interrupt) anything else on the shared browser. `startMonitorSchedule()` also immediately enqueues one check so the UI shows real data right away instead of waiting a full interval. `stopMonitorSchedule()` halts future ticks only — does not forcibly kill an in-flight check, documented as intentional (safely cancelling mid-browser-session is a bigger feature than this dev tool needs) | Reuses exactly the mechanism the user asked for reused. **Caught and fixed a real bug during verification**: `getJobSchedulers()`'s entries expose the scheduler's own identifier as `.key`, not `.id` (`.id` is a separate, unrelated field, `null` here) — confirmed by querying a live queue directly rather than trusting the type definition's field ordering. The first version checked `.id` and always reported `running: false` even with the scheduler genuinely active; fixed to check `.key`, re-verified (start → `running: true` → wait past the interval → `lastCheckedAt` advances with zero manual trigger → stop → no further advances → full Control Panel process restart → state and scheduler status both correctly preserved from Redis/the JSON file, not lost). |
| XC Bank Monitor — dedup verification approach | Verified by construction (a `Set`-backed filter — architecturally impossible to double-add an already-seen ref) plus empirically across 4 consecutive real checks, refs accumulating monotonically by exactly each check's new-transaction count (7→14→20→28), `seenRefs.length` staying in lockstep with `notifications.length` throughout | An exact "two checks land in the identical 10-second window" test turned out impractical to trigger for real: each check's own round trip (container spin-up + browser navigation) takes 5-8s, comparable to the window itself, and XC Bank's transaction refs embed the window's seed, so distinct windows never produce colliding refs by the mock's own design. The chosen verification (code-level guarantee + monotonic real-check evidence) is equivalent proof without fighting unrepresentative timing. |
| XC Bank Monitor — 200-screenshot retention, verified without 200 real checks | Seeded `data/monitor-state/xc-bank.json`'s `screenshots` array directly (205 entries, 10 backed by real placeholder files at the removal boundary, 2 also uploaded to MinIO), then triggered retention via one real `checkOnce()` run (pushing the total to 206) | A real check loop at the ~20s dev interval would take over an hour for 200 iterations. Confirmed exactly 200 remained, precisely the correct 6 oldest-by-position files were deleted from local disk, and both MinIO objects among them were gone too (`GET /api/artifacts/screenshots/<filename>` 404s) — the retention *code path* itself ran for real, only the seed data was synthetic. |
| XC Bank Monitor — readable-failure verification | Stopping `browser-worker-chrome` didn't actually test a failure path — `docker compose run --rm worker ...`'s `depends_on: browser-worker-chrome` auto-restarts it. Stopped `xc-bank` itself instead (not a `worker` dependency, no auto-heal) | Confirmed a real failure (`net::ERR_ADDRESS_UNREACHABLE`) surfaces as a clear `lastError` string, the Control Panel process and all its other routes (`/api/status`, `/api/monitors/xc-bank`) stayed fully responsive throughout, and a subsequent check succeeded normally once `xc-bank` was restarted — proving genuine recovery, not just that the error path exists. |
| XC Bank Monitor page | New, separate `/monitors/xc-bank` (`xc-bank-monitor.html` + `.js`, its own small polling script rather than growing `app.js`) — explicit user instruction to leave the existing noVNC take-control page at `/` alone; only a single link added there | Read-only status view (status/balance/notifications/transactions/screenshot timeline, Start/Stop/Check-once buttons) — not a browser-control surface, matching the explicit "not a noVNC page" requirement. |
| Monitor UI bug — relative script path | `/monitors/xc-bank` never actually rendered anything dynamic: `<script src="xc-bank-monitor.js">` is a *relative* path, and at URL `/monitors/xc-bank` (no trailing slash) a browser resolves that relative to `/monitors/`, requesting `/monitors/xc-bank-monitor.js` — 404. The HTML shell (title, static labels, empty-state text) rendered fine, so the page looked "present but broken" rather than erroring visibly, which is why it read as "not showing" rather than "showing an error." Fixed with an absolute path, `/xc-bank-monitor.js` | Root cause reproduced empirically before any fix was written — curled the live page, confirmed the HTML's exact script tag, confirmed the resolved URL 404s and the actual file 200s at the real location. Re-verified after the fix by running the page's *actual* JS (not a rewritten copy) against real API data in a Node harness with a minimal DOM stub, confirming every URL the render functions produce (image `src`, click-through `href`, MinIO link) resolves to 200 — stronger evidence than "the page loads" alone. |
| Screenshot timeline — real thumbnails | `xc-bank-monitor.js`'s `renderScreenshots()` now renders an actual `<img>` per entry (via the existing local `/screenshots/:filename` route, CSS-capped to a small thumbnail size) wrapped in a link that opens the full-size image in a new tab, instead of two bare text links | Matches the explicit ask for a genuine visual timeline, not just links. All entries render at once (up to the existing 200-item retention cap from the prior round, untouched) — 200 small thumbnails is a reasonable page weight, no pagination needed. The MinIO link stays as a small secondary text link, still only ever through the existing server-side-proxied `/api/artifacts/screenshots/:filename` route. |
| Monitor registry — scope | New `services/control-panel/src/monitors-registry.ts`: a thin, data-driven *listing* layer (`MonitorSummary`, one `getSummary()` function per registered monitor, `listMonitorSummaries()`) powering `GET /api/monitors` (new) and `/`'s Monitors section — deliberately **not** a refactor of `monitor.ts`'s underlying check/dedup/retention logic into a generic multi-site engine | Only one real monitor exists; abstracting the check engine itself for hypothetical future sites would be premature. Adding a second monitor later means writing its own state module + `getSummary()` + one more registry entry — `/`, `app.js`, and `GET /api/monitors` never need to change again. Start/Stop/Check-once for a listed monitor follow the convention `/api/monitors/<id>/start\|stop\|check-once` (not re-declared per-entry in the registry) — the same shape XC Bank's own routes, already built in the prior round, naturally have. |
| `/` as Control Center | Existing Browsers/Workflows/Jobs sections and the noVNC take-control flow are completely unchanged (confirmed via diff review); the intro paragraph now describes the page as the overall control center, and the previously-static single XC Bank link became a dynamic, data-driven "Monitors" section (`app.js`'s new `loadMonitors()`/`renderMonitors()`, same polling pattern as the existing `pollStatus`/`pollJobs`) | Delivers "control center that can show multiple monitors" without touching the parts of the page the user explicitly said not to change much. Each monitor card shows name, a status dot (red specifically when `lastError` is set, distinct from plain stopped), summary, last checked, and Start/Stop/Check-once buttons — verified for real: a card's dot went from green to red and the summary/last-checked fields updated correctly when `xc-bank` was stopped and a check failed, then back to green with a real balance once restarted. |
| Monitor gets two views: history/detail vs. live/current | `/monitors/xc-bank` stays the history/detail view (screenshot timeline, transaction history, notification history) unchanged; new `/monitors/xc-bank/live` is a separate, purely additive "live operation" view — two columns, left is the live browser, right is a compact live-updating extracted-data panel. `monitors-registry.ts`'s `MonitorSummary`/`MonitorDefinition` gained a `livePath` field alongside `detailPath` so both links render on `/`'s Monitors section and any future monitor gets both automatically | The two views serve genuinely different purposes — "what has the bot found over time" vs. "what is the bot doing right now" — and conflating them into one page would make both worse. No new API routes needed for either; both pages are thin frontends over `GET /api/monitors/xc-bank` (already returns everything each panel needs) and `GET /api/status`. |
| Live view — reused the existing noVNC endpoint instead of a new streaming mechanism | The live page's left column embeds `http://localhost:6080/vnc.html` — the exact same URL `/`'s own "Take control" button already uses — rather than building a new screenshot-streaming or websocket-based live-view mechanism | This is genuinely the same shared, concurrency-1 browser the monitor's own checks drive (confirmed by reading the queue/exec code, not assumed), so it's a real live view, not a simulation, with zero new plumbing. A second, independent iframe embed doesn't interact with or break the existing take-control iframe on `/` — verified by leaving `/` completely untouched aside from the two new Detail/Live links. |
| Live view — Chrome-not-running fallback | When `GET /api/status` reports Chrome not running, the live page swaps the iframe for a guidance panel (a "Start Chrome" button wired to the existing `POST /api/action/startChrome`) plus the most recent screenshot from monitor state (`state.screenshots[0]` — confirmed newest-first via `monitor.ts`'s `unshift()`, not assumed) | Chrome being down is routine in dev (nothing auto-starts it) — a live page that just goes blank in that case would be a worse regression than the original bug this whole feature area started from. Reusing the existing `startChrome` action and the monitor's own screenshot history means zero new backend code for the fallback. |
| Live view — no-flicker polling | The iframe's `src` is assigned only on the stopped→running *transition* (tracked via a `chromeShown` flag in `xc-bank-monitor-live.js`), never on every 3s poll tick; the fallback screenshot's `<img src>` is only updated when the latest filename actually changes, not on every poll | A live page that reloads its iframe or re-fetches an unchanged image every 3 seconds would be distracting/flickery in exactly the way the user asked to avoid. Verified with a Node `vm` harness that loads the real, unmodified `xc-bank-monitor-live.js` against a stubbed DOM, calls its exposed `fetchStatus()` three times in a row against the live API (Chrome genuinely running throughout), and counts iframe `src` assignments — confirmed exactly 1, not 3. Also verified the Chrome-stopped branch directly against the live API: the guidance panel and a real, 200-resolving fallback screenshot both render correctly (harness output, not assumed). |
| "Polite automation" — scope, framed explicitly as not a bypass | Four measures: per-site policy config (locale/timezone/pacing), a raw-CDP locale/timezone override, randomized action/typing delay, and a basic challenge-page (CAPTCHA/verification/2FA text) detector that stops rather than attempts to solve/bypass. **Explicitly excluded**, per direct user instruction: `navigator.webdriver`/CDP-artifact patching, mouse-movement humanization, and any proxy/residential-IP work — those were discussed as options but this round is scoped to false-positive reduction only, not fingerprint evasion | The user asked for "not too much" and drew the line themselves at legitimate pacing/localization vs. techniques that would misrepresent what the automated client actually is. Keeping that line explicit in code comments and docs (not just this row) matters because the boundary is easy to blur incrementally later. |
| Locale/timezone via raw CDP, not `newContext()` | `run-workflow.ts` reuses the browser's *existing* default context/page rather than creating a fresh one per run, so Playwright's `newContext({locale, timezoneId})` (context-creation-time only) doesn't apply. Uses `context.newCDPSession(page)` + `Emulation.setTimezoneOverride`/`setLocaleOverride` instead, in a new `apply-policy` step right after `connect` | Same "drop to raw CDP for something Playwright's high-level API doesn't expose on an already-existing context/page" pattern already discussed elsewhere in this repo (the deferred downloads fix) — not a new architectural direction, a consistent one. |
| `Emulation.setLocaleOverride` doesn't change `navigator.language` — found empirically | Verified directly, not assumed: after `setLocaleOverride({locale: "th-TH"})`, `page.evaluate(() => navigator.language)` still returned `"en-US"`. `navigator.language`/`navigator.languages` come from Accept-Language, set via `Emulation.setUserAgentOverride({userAgent, acceptLanguage})` instead — re-verified after the fix, `navigator.language` correctly reported `"th-TH"` | `setUserAgentOverride` is called with the **real** `navigator.userAgent` read back from the page and passed through completely unchanged — only `acceptLanguage` is set. This is a language-preference correction, not a browser-identity spoof; the actual Chrome UA string sent never differs from the genuine one. |
| Action delay + typing delay scope | A randomized `page.waitForTimeout()` (from the resolved policy's `actionDelayMs` range) before any `navigate`/`dismissPopup`/`login`/`xcBankLogin`/`xcBankLogoutClean` step (i.e. anything that clicks/navigates) — not before read-only steps (`extract`/`screenshot`/`saveSession`/`xcBankExtractDashboard`), which cause no page transition. Typing itself (`policy.ts`'s `humanFill()`, wrapping Playwright's `locator.pressSequentially(text, {delay})`) replaces `page.fill()` in exactly the two places user-facing credentials are typed: `actions/registry.ts`'s `login` handler and `adapters/xc-bank.ts`'s `login()` | Contained scope matching the "ไม่บาน" (don't let it balloon) instruction — one hook point in the workflow loop for pacing, two call sites for typing, rather than threading delay logic through every handler individually. |
| Challenge detector — detect and stop, never bypass | `challenge.ts`'s `detectChallenge()` checks rendered page text against a small keyword list (`captcha`, `verification`, `verify your identity`, `2fa`, `two-factor`) after any `navigate`/`login`/`xcBankLogin` step; on a match, throws a step error explicitly stating "not attempting to bypass" rather than trying to solve/click through anything | Flows through entirely existing machinery — a real challenge on an XC Bank monitor check becomes a normal, readable `lastError` string with **zero** monitor-side code changes, since the monitor's checks already run through this same `run-workflow.ts` engine. Verified both branches with `page.setContent()`: challenge text → matched; real XC Bank dashboard content → `null` (no false positive), confirmed via a real end-to-end `xc-bank-login-extract` run afterward. |
| Monitor scheduling jitter, not workflow-engine jitter | `services/control-panel/src/queue.ts` gained `MONITOR_JITTER_MS` (default 5s) — a random sleep *before* `checkOnce()` runs, but only for jobs the scheduler itself produced (tagged `data: {scheduled: true}` via `upsertJobScheduler`'s job template). Manual "Check once" and the immediate first tick after "Start" stay untagged and instant | This lives in the BullMQ scheduling layer, not `run-workflow.ts`, because it's about *when a check starts*, not *how it behaves once running* (that's the per-action delay above, which the monitor already inherits for free since its checks run through the workflow engine). A user explicitly clicking "Check once" expects an immediate response — jittering that would just look like the UI being slow, not politeness. Verified for real: consecutive scheduled-tick gaps measured 15.5s and 18.75s around a nominal 20s interval (not identical, consistent with jitter), while a manual check-once completed in ~3.2s with no added delay. |
| Pause vs. Stop, as distinct concepts | Stop (`stopMonitorSchedule`) removes the BullMQ scheduler entirely — resuming means recreating it. Pause is new and lighter: a `paused` boolean on `MonitorState`. The scheduler keeps running (so `next`/`iterationCount` stay meaningful, and the UI's "next check" estimate stays accurate even while paused); a **scheduled** tick checks the flag and skips `checkOnce()` if paused; a **manual** "Check once" always ignores `paused` and runs — same "manual is always authoritative" precedent the jitter work already established | Pause exists for a real workflow the jitter/pacing work made newly relevant: taking manual control (noVNC) of the shared browser without the bot fighting for it mid-session, without losing the schedule's timing or needing to fully restart it afterward. |
| Pause/resume/cleanup routed through the queue, not direct file writes — a real bug found and fixed | First version called `setPaused()` directly from the API route (reasoned: "a simple idempotent flag, no browser/file side effect to race"). **That reasoning was wrong, caught by a real test**: paused immediately after triggering a check, then queried — `paused` had silently reverted to `false`. Root cause: `checkOnce()` loads its own `MonitorState` copy at the start of a multi-second real-browser check and writes the *whole object* back at the end; a concurrent, out-of-band `setPaused()` landing inside that window gets overwritten by `checkOnce()`'s own stale in-memory copy on save — the same class of risk already correctly identified and avoided for `cleanup` from the start, just missed for pause/resume. Fixed by adding an `xc-bank-monitor-set-paused` job type, processed through the same concurrency-1 queue as `checkOnce()`, so the two can never interleave | Re-verified after the fix with the exact failing scenario repeated (manual check-once, then pause fired immediately after while the check was still active) — this time the pause job correctly queued behind the in-flight check and applied cleanly afterward, confirmed via both the API state and the two jobs' completion order in `/api/jobs`. |
| Next-check estimate and long-running warning need no new tracking | `nextCheckEstimate` comes directly from `getJobSchedulers()`'s own `next` field (confirmed against a live queue before relying on it, same as `every` was in the jitter round). `longRunningWarning` is derived purely from existing state: the oldest screenshot's `capturedAt` (screenshots are newest-first via `unshift()`) and the count nearing the 200-item retention cap | Both signals already existed inside data the monitor was already tracking for other reasons — no new persisted fields, no new polling. Verified live rather than synthetically seeded: a monitor that had genuinely been running for over an hour already showed `"Running a while: 200/200 screenshots tracked, oldest from 1.1h ago"` without any test setup. |
| Bulk pause/stop via the existing registry extensibility point | `monitors-registry.ts`'s `MonitorDefinition` gained `pause`/`stop` function fields alongside the existing `getSummary` — `pauseAllMonitors()`/`stopAllMonitors()` (`Promise.allSettled` over `MONITORS`, one monitor's failure doesn't block the others) mirror `listMonitorSummaries()`'s existing per-entry-independent shape | A future second monitor wires its own `pause`/`stop` into the same registry entry it already needs for `getSummary` — `/`, `app.js`, and the two new bulk routes never need to change again, matching the registry's original design intent from the Control Center round. |
| Gmail/Phase 3 stays paused — explicit user direction | This round (Health/diagnostics) was explicitly chosen *instead of* continuing Phase 3, not because Phase 3 hit a blocker | No Gmail/OAuth/email code touched this round; the existing scaffold from earlier sessions is untouched. Resume only on explicit future direction. |
| Health checks: queue-worker detection via `queue.getWorkers()` | The API process and the separate `npm run worker` process share no IPC channel — `getWorkers()` (BullMQ's own mechanism, backed by Redis `CLIENT LIST`) is the only real signal. Confirmed live before relying on it: returns one entry while the worker process is running, empty when stopped | Verified both directions for real (not just code-reviewed): stopped the worker process, confirmed the check went `error` and an enqueued job genuinely sat in `waiting`; restarted the worker, confirmed the check went `ok` and the waiting job completed. |
| Health checks: bounded-timeout Redis ping, not a bare await | ioredis queues commands during a real connection outage rather than failing fast (a gotcha already documented elsewhere in this repo for `/api/jobs`) — a diagnostics check must not reproduce that hang. `checkRedisHealth()` races `client.info()` (not `.ping()` — BullMQ's `IRedisClient` abstraction type doesn't declare `ping()`, only commands BullMQ itself needs internally; `info()` is declared and is an equally real round trip) against a ~1.5s timeout | A health page that itself hangs for 20+ seconds when Redis is down would defeat the entire point of the page. |
| Health checks: same AggregateError-empty-`.message` fix, found again independently | `checkMinioHealth()`'s failure against a real stopped MinIO container came back as `"AggregateError"` (a useless message) until traced to the same root cause already fixed once for the artifact route: a connection-refused failure is a Node `AggregateError` with an **empty** top-level `.message`, but a real `.code` (`"ECONNREFUSED"`). Fixed with the same `.message \|\| .code \|\| String(err)` fallback | Not copy-pasted from memory — reproduced and confirmed directly against a real stopped MinIO container before and after the fix, same empirical standard as the original fix. |
| Health checks: text-only fix hints, no "run it for me" button | Every failing check's `hint` field is a plain shell command string, rendered in a `<code>` block — never wired to any action that executes it | Explicit, repeated user instruction: readiness checks must never auto-start anything silently. The only executable action anywhere in this round's UI is the read-only "Run readiness check" button itself (which only re-runs the checks). |
| Health checks: MinIO down is annotated as non-fatal, not treated as equally severe to Redis/worker down | The MinIO check's error message gets `" — artifact archival will error, but the main job can still run"` appended | Verified for real, not just asserted: with MinIO stopped, a `demo` workflow job still completed successfully (`ok: true`) with only its `archive-screenshot` step failing — matches `stepBestEffort()`'s existing architectural guarantee from the original MinIO-archival round. |
| Clean/build scripts, one shell each, no combined "do everything" script | `clean.sh`/`clean.ps1`/`clean.bat` and `build.sh`/`build.ps1`/`build.bat` at repo root each do exactly one already-documented `CleanAll.md` step (`--rmi local` removal / rebuild) — no new flags, no `--rmi all` or dev-data-wipe variant scripted, no auto-redeploy chained on afterward | Matches `CleanAll.md`'s own tiered structure (the two riskier/heavier steps stay manual-only) and the project's established "explicit action per button/script" posture from the Health/diagnostics round. All 6 scripts were run for real (not just written), each exactly once, ending in a working redeployed stack. |
| `clean.sh`/`build.sh` committed as `100644` (non-executable) — found and fixed | `core.fileMode=false` on the Windows dev machine that authored them meant a local `chmod +x` never got staged; `git ls-files -s` confirmed `100644` after the first commit. Fixed with `git update-index --chmod=+x <file>`, which sets the tracked mode directly regardless of `core.fileMode` — `git ls-tree` re-confirmed `100755` afterward | A Linux clone would otherwise have hit `Permission denied` on `./clean.sh` until a manual `chmod`. Same fix applied proactively to `up.sh` when it was added, so it never shipped with the wrong mode in the first place. |
| `up.sh`/`up.ps1`/`up.bat` added, deliberately separate from `build.*` | A third single-purpose script (`docker compose up -d redis minio xc-bank browser-worker-chrome`, nothing else) rather than folding "start after build" into `build.*` itself | User's own reasoning, confirmed as the direction: `clean`/`build`/`up` are three separately-composable `CleanAll.md` steps, matching the existing tiered-script pattern instead of one "do everything" script. Deliberately does **not** start `npm start`/`npm run worker` — those are meant to run in their own visible terminals per `StepByStep.md`, and silently backgrounding them would contradict the explicit no-silent-auto-start posture from the Health/diagnostics round. |
| Monitor auto-stop — separate job type, not folded into pause | New `xc-bank-monitor-set-autostop` BullMQ job type, kept deliberately separate from the existing (already-tested) `xc-bank-monitor-set-paused` | Reuses the exact queue-serialization lesson from row 98 (pause/resume) rather than re-deriving it, without risking regression to the working pause mechanism by touching its job type. A scheduled tick checks `autoStopAt` before `paused` in one `loadState()` call; manual "Check once" ignores both, same "manual is always authoritative" precedent as jitter/pause. |
| Monitor auto-stop — verifying the scheduler was genuinely removed | `running` in every monitor API response is read live from BullMQ's own `getJobSchedulers()` (via `getMonitorScheduleInfo()`), never a stored boolean | This means `running: false` after an auto-stop tick fires *is itself* the proof the BullMQ scheduler was actually removed, not just that a flag was set — verified live with a real 1-minute `autoStopMinutes` timing test (scheduler gone, `autoStopped: true`, manual check-once still worked immediately after, starting again cleared `autoStopped`). No separate "did it really stop" check was needed beyond the field that was already there. |
| Page reset per workflow run — `newPage()` not `newContext()` | `run-workflow.ts` closes every existing page in the shared context (best-effort) and opens exactly one fresh page via `context.newPage()`, wrapped in a real `step()` (must succeed) | Confirmed empirically via direct CDP testing before choosing this: a same-context `newPage()` inherits the parent window's current maximized state for free; `browser.newContext()` instead opens a genuinely separate top-level window that does not. Verified live post-implementation: `GET /json/list` on the real CDP endpoint showed exactly 1 page target after 4 back-to-back workflow runs, never accumulating. Documented, not solved: no reliable signal exists for "a human is using noVNC right now," so a queued job resetting the page mid-manual-session remains a pre-existing risk. |
| Window-size fix — original hypothesis disproven, fallback proven harmful | Planned fix (force `windowState: "normal"` then explicit `{left,top,width,height}` bounds on a new page, on the assumption new tabs open undersized) was tested directly via CDP before implementing anything, per explicit user instruction to test first. Result: a fresh `newPage()` already reported `windowState: "maximized"`, `1366x748` — the "opens small" premise was wrong. The planned fallback was then tested anyway and found to **actively shrink** an already-maximized window (`1366x748` → `1366x726` `"normal"`) | Scope reduced to observability-only per the user's own revised direction after seeing this result: a harmless best-effort idempotent `setWindowBounds({windowState:"maximized"})` call (separately confirmed *safe* but *not* reliably effective at recovery), plus measuring and logging real `window.innerWidth`/`innerHeight` (flagging, never correcting, if below ~1200×600 against the real 1366×768 Xvfb resolution). **The ruled-out fallback must not be reintroduced** — recorded here specifically so a future session doesn't re-attempt the same disproven fix. |
| Chromium silent-crash blind spot — recurred during this round's own verification | While verifying the above three items, Chromium inside `browser-worker-chrome` silently died three more times within roughly 15 minutes of real (rapid/concurrent) use on this dev machine — no OOM (`docker stats`/`OOMKilled: false`), no fatal entry in `/var/log/browser.log`, container itself stayed `"Up"` throughout each time exactly like the original 38-hour incident that started this whole round | Not fixed this round — same known gap as before (no CDP-reachability check anywhere; `/api/health`'s noVNC check only pings the noVNC HTTP endpoint, which stays up even when Chromium itself is gone). Recurring this fast under load (not just after 38h idle) raises the priority of eventually adding a real `GET {CDP_URL}/json/version` check to `/api/health`, but that decision is still explicitly the user's to make, not assumed here. |
| Multi-lane (per-account/per-site browser isolation) — design doc only, no implementation | User redirected the "next: multi-lane" direction to require a written design first, specifically because of the recurring Chromium-crash blind spot above — a naive multi-lane rollout (N independent browser containers) would multiply that same blind spot N times if lane health/recovery isn't designed in from the start, not bolted on after. Full design in [`docs/BOT_LANE_ISOLATION.md`](./BOT_LANE_ISOLATION.md): lane model (one browser container per site+account, no shared context/profile/session/state/artifact-prefix), one BullMQ `Queue`+`Worker(concurrency:1)` pair per lane (not one shared queue routed by laneId) so lanes run genuinely in parallel without one lane starving another, a `LaneHealth` shape whose key new signal is a real per-lane `GET {cdpUrl}/json/version` check (the thing that actually closes the blind spot — container-`Up` status alone is proven insufficient), an explicit (never silent) per-lane Restart button, and a 5-step incremental migration (single-lane registry → lane health/CDP-reachability → state/artifact path isolation → second-lane proof → parallel-execution proof) | Explicit instruction: no runtime multi-lane code this round, design doc only, commit + push the doc. Grounded directly in the current codebase (`docker-compose.yml`, `queue.ts`, `monitor.ts`, `health.ts`, `cdp.ts` all read in full while writing it) rather than written in the abstract, so the migration steps reference real files/patterns that already exist. |
| Real-bank site (`scb-business-anywhere` / scbbusinessanywhere.com) — authorization confirmed before any code | User asked to add automation for a real, live bank site — materially different from XC Bank (a mock built specifically to sidestep this exact question). Paused and asked directly: is this your own account, and do you accept the real ToS/ban/security-risk consequences of automating a live bank (unlike a mock, real regulatory/account-suspension risk applies). User confirmed: own business account, risk understood | This is a hard line this project holds generally (real financial institutions, real money, real ToS) — proceeding required an explicit, on-record answer, not an assumption, before writing a single line of code that touches the real site. |
| Real-bank site — isolated lane built *before* any login/DOM work, not after | Rather than exploring the real site's login flow on the existing shared `browser-worker-chrome` (used by XC Bank/testing), a genuinely separate lane was stood up first: `browser-worker-scb-business-anywhere-1` + `worker-scb-business-anywhere-1` in `docker-compose.yml` (own Chromium container, own `data/lanes/scb-business-anywhere-1/{profile,output,sessions}`, own noVNC on `127.0.0.1:6090` — loopback-only, tighter than the existing chrome/firefox port bindings). `GET /monitors/scb-business-anywhere/live` (new static page) embeds only this lane's noVNC; a new "Lanes" section on `/` mirrors the existing Browsers section's Start/Stop pattern via `actions.ts`'s allowlist (`startScbLane1`/`stopScbLane1`) | User's own explicit redirect, mid-session, after a first exploratory (read-only, no-credential) run had already happened on the *shared* browser: "อย่าใช้ shared Chrome เดิมกับ SCB" (don't use the existing shared Chrome for SCB). Isolation here is structural, not procedural — `queue.ts` has zero references to `worker-scb-business-anywhere-1`, so a scheduled XC Bank job literally cannot reach this lane's browser, verified by code inspection, not just by convention. Verified live: separate noVNC port (6090) responds, separate profile directory confirmed on disk, `demo` workflow re-run against the original shared lane afterward to confirm it was completely unaffected. |
| Real-bank site — no stealth/fingerprint-evasion, asked and answered directly | User asked directly whether the current Chrome setup is "เนียนพอ" (convincing/stealthy enough) to avoid a real bank's fraud/bot detection. Answered directly: no, and this project will not build genuine detection-evasion techniques (browser fingerprint spoofing, `navigator.webdriver` hiding, proxy/IP masking, behavioral humanization beyond the existing pacing) targeting a live bank's fraud controls | Same "polite automation, not bypass" line already drawn project-wide (see the original "polite automation" decision row above) — restated explicitly because the stakes are categorically higher against a real bank than XC Bank/the-internet.herokuapp.com. Recommended path instead: human handles login/OTP entirely via manual noVNC takeover on the new isolated lane; any future automation stays narrow/read-only on top of an already-human-authenticated session, never attempting login or a challenge step itself — consistent with the project's existing, non-negotiable "CAPTCHA/2FA/passkey = stop and hand off to a human" rule. |
| Real-bank site — deliberately stopped short of login automation this round | Only a read-only `scb-business-anywhere-explore.json` workflow exists (`navigate`+`screenshot`, zero credentials, zero form interaction) — used once, before the isolated lane existed, purely to confirm the real login page loads and is a two-step username-then-password flow. No adapter, no credential handling, no `.env` entries, no monitor/check logic for this site exist yet | Explicit user instruction: isolated lane + live page shell only this round; next step is gated on the user manually logging in once via the new lane's own noVNC (`:6090`) to reveal the post-username page structure without any credential ever passing through the bot, and on their explicit go-ahead for what (if anything) gets automated next. |
| Real-bank site — bot must never type credentials, even with a human only watching | User asked whether the bot could type the username/password itself while they just watch (no clicking). Declined directly: (1) the real password would have to pass through the bot/workflow/logs at some point, which this project refuses for a real bank account; (2) counter-intuitively, a *human* typing is more detection-resistant than automation typing, since mouse/keyboard/timing patterns differ and a bank's fraud engine is far more likely to flag automated input than manual input — automating just the typing step would *increase* risk, not reduce it | Directly extends the existing "CAPTCHA/2FA/passkey = human only" rule to cover the entire login step for this specific real-bank lane, not just the challenge/OTP portion. |
| "Assisted Manual Login" view — noVNC embedded directly in the Control Panel page, `autoconnect=true` | Rather than requiring the user to separately open `http://localhost:6090/vnc.html` in another tab, `scb-business-anywhere-live.html`'s iframe points at `.../vnc.html?autoconnect=true` — skips noVNC's own "Connect" button click. The user still types this lane's own local noVNC access password (`VNC_PASSWORD`, a dev-only environment password already documented throughout this repo) once via noVNC's built-in prompt — that is not the bank credential and was never a secret this project was trying to protect | Directly serves the user's own explicit requirement: "หน้า live ต้องทำให้ผมไม่ต้องเปิด noVNC แยก" (the live page must mean I don't have to open noVNC separately). No new secret-handling code was needed since the noVNC password isn't a bank credential. |
| "Assisted Manual Login" — two narrow bot actions only, backed by genuinely different execution paths | **Open Login Page** runs the normal `run-workflow.ts` engine (via a new minimal `scb-business-anywhere-open-login.json`, `navigate` only) against `worker-scb-business-anywhere-1` — safe because it always resets to a fresh page first, which is *only* correct to do before a human has logged in. **Analyze current page** deliberately does **not** use `run-workflow.ts` at all — a new standalone script, `services/worker/src/analyze-page.ts`, connects and reads `context.pages()[0]` exactly as left (URL/title/visible-text-snippet/screenshot) without ever calling `newPage()` or navigating, because doing so after a manual login would discard the very session the human just created. Prints a `LANE_PAGE_ANALYSIS {...}` stdout marker, same convention as `XC_BANK_DASHBOARD` | The two actions have fundamentally different correctness requirements (one must reset, the other must never touch the existing page) — reusing one engine for both would have meant either breaking manual sessions on every "analyze" click, or adding a reset-skipping flag to the shared engine that every *other* workflow would then have to be careful not to accidentally trip. Two small, purpose-built scripts stayed simpler and safer than one generalized one. |
| Real-bank lane-specific workflows excluded from the generic Workflows section | `exec.ts`'s `listWorkflowNames()` filters out any workflow file whose name starts with `scb-business-anywhere` before returning the list `GET /api/workflows` (and thus `/`'s generic Workflows section, and `/api/enqueue-workflow/:name`'s own validation) ever sees | Found while wiring the Assisted Manual Login buttons: the generic Workflows section always enqueues against the *shared* `worker` service (`queue.ts`'s `enqueueWorkflow` → `runWorkflow()`, hardcoded to `"worker"`), so leaving these two files discoverable there would have let someone accidentally run real-bank navigation on the wrong (shared) browser — a real isolation break, not just visual clutter. Excluded structurally (the API itself refuses the name) rather than just omitted from a UI list. |
| Real credential incident — declined twice, `.userpass` hardening, no values recorded here or anywhere in code | User asked to read a local `.userpass` file and use it to auto-fill the real SCB login form ("everything except withdraw money"); declined — explained the risk isn't what the bot does *after* login, it's that a real password passing through the bot/conversation at all is a permanent-ish exposure, and that bot-driven typing is more detection-prone than manual typing regardless of scope. User then pasted the real username/password directly into chat. Declined to use it, did **not** echo it back, did **not** write it to any file, and recommended rotating that bank password immediately since chat history isn't an appropriate place for it to have ever existed | `.userpass` was found untracked and **not** in `.gitignore` (real risk of an accidental future commit) — fixed immediately, confirmed via `git log --all -- .userpass` it had never actually been committed. New `.userpass.example` (placeholder values only, safe to commit) documents a multi-site block format (`website:`/`username:`/`password:`, blank-line-separated) for the user's own manual reference — explicitly for *typing into the Assisted Manual Login noVNC screen themselves*, the bot still never reads `.userpass` for any site, automatically or otherwise. |
| Real credential incident, continued — every rephrasing declined (loop script, Telegram relay, encrypt/decrypt) | User asked, across several follow-ups, for the same underlying thing in different forms: an auto-relogin loop reading `.userpass`, relaying credentials/OTP through a Telegram bot into the site, and storing credentials encrypted-at-rest so the bot could decrypt-and-type them. All declined, each for the same core reason restated per form: a loop would make repeated-automated-login *more* fraud-detectable, not less, and would still eventually need to submit OTP (hard-prohibited); a Telegram relay adds a third party (Telegram's own servers) to the exposure without removing it; encryption-at-rest solves a storage-security problem, not the actual concern (autonomous credential *use*/typing is what's detection-prone, regardless of how it was stored beforehand) | Recorded explicitly so a future session doesn't treat a differently-worded version of the same request as a novel question needing fresh deliberation — the answer is the same regardless of the specific mechanism proposed, because the underlying action (bot autonomously enters real bank credentials) is what's declined, not any one implementation of it. |
| XC Bank Monitor → Telegram notifications | New `services/control-panel/src/telegram.ts`: `sendTelegramMessage()`, best-effort (same posture as MinIO archival's `stepBestEffort`), no-op if `TELEGRAM_BOT_TOKEN_XC`/`TELEGRAM_CHAT_ID_XC` are unset. Wired into `monitor.ts`'s `checkOnce()` (one summary message per check when new transactions are found, not one per transaction) and `queue.ts`'s auto-stop trigger (fires when a scheduled tick detects `autoStopAt` elapsed). **Notification-only** — never receives input, never used to relay credentials anywhere, explicitly separate from the credential-relay requests declined above | Directly requested, and clearly a different, safe category from the declined requests: one-way outbound alerts carry none of the "bot handles a real credential" risk. Verified for real: a direct API call with the real token/chat_id succeeded (message delivered to the actual Telegram chat), and the real `sendTelegramMessage()` module (not a reimplementation) was invoked directly via `tsx` and completed without error. |
| Host processes never actually loaded `.env` — found and fixed | `services/control-panel/src/server.ts`/`worker.ts` (the two host `npm start`/`npm run worker` processes) had **no** `.env` loading at all — every `process.env.X` read elsewhere (e.g. `artifacts.ts`'s `MINIO_ROOT_USER`) silently fell back to its hardcoded default, which happened to equal `.env.example`'s own default value, masking the gap. `docker-compose.yml`'s own built-in `.env` loading (for the *containerized* services) never covered these two host processes. Fixed with a new `dotenv`-based `src/env.ts`, imported first thing in both entry points | Found while wiring Telegram notifications — `TELEGRAM_BOT_TOKEN_XC` needed to reach the host queue-worker process somehow, and there was no existing mechanism for that. This also retroactively fixes the latent MinIO-credential gap: changing `MINIO_ROOT_PASSWORD` in `.env` now actually takes effect for the host processes, not just the Docker services. |
| SCB balance/transaction monitor — built only after real manual login, live-iterated with the user | The user manually logged into their real SCB Business Anywhere account via the Assisted Manual Login noVNC embed, then walked through the real authenticated dashboard together with the assistant in real time (company switcher, Account Summary nav, a real -300.00 THB transaction, its expanded detail view) before any extraction code was written — every DOM interaction pattern below was confirmed against the live site first, never guessed | Same empirical-before-implementing discipline used throughout this project (e.g. the window-maximize investigation), applied to a real, unfamiliar, unknown-structure production site instead of a controlled mock — there was no other way to know the real click paths (e.g. "Account Summary" sometimes lands on an overview needing an extra "View Details" click, sometimes not) without observing them directly. |
| SCB company switch — `page.getByText` click, not `page.goto()` | `select-company.ts` clicks the named entry in the (reopened if needed) company-switcher dropdown, an in-app SPA click | A `page.goto()` triggers a real full-page reload; the currently-active company selection is session/client-side state, not encoded in the URL, so a reload risked silently resetting back to a default company and reporting the wrong one's data. Confirmed by observation: the working company-switch flow used earlier in the session was a real click, and stayed on the selected company across subsequent same-session actions. |
| SCB non-ASCII (Thai) values passed base64-encoded through `execFile`, not as plain argv | `runScbSelectCompany()` (`exec.ts`) base64-encodes the company name before passing it as `COMPANY_NAME_B64`; `select-company.ts` decodes it | Found empirically while debugging a real failure: a Thai company name passed as a plain `-e COMPANY_NAME=...` argument through Node's `child_process.execFile` on Windows arrived at the child process already corrupted into literal `?` characters. Root-caused by testing the same value two ways — a direct `docker compose run` invocation via the Bash tool worked fine, but the value passed through `execFile` did not — isolating the corruption to Windows argv marshalling for non-ASCII text specifically, not to docker, not to the site, and not to the real browser UI (which sends `fetch()` bodies as proper UTF-8 and was never affected). Base64 is pure ASCII, sidestepping the issue regardless of host codepage. |
| SCB transaction/balance extraction — labeled regex on `innerText`, not DOM selectors | `check-transactions.ts` extracts balance figures and Latest-Transactions rows via per-label regex against the page's visible text (e.g. `Available Balance\s*\n+\s*([\d,]+\.\d{2})\s*THB`), the same technique already used for XC Bank's own balance figures earlier this session | No reliable CSS selectors/test-ids are available for a real, unfamiliar production site the way there are for XC Bank's own controlled mock — text-pattern extraction against content already fetched for the page is more resilient to markup changes and faster to build safely than reverse-engineering a real site's DOM structure. |
| SCB transaction dedup — composite key, not a single ref/id | `scb-monitor.ts` dedups new transactions by `date\|time\|trCode\|description\|amount` concatenated, not a single unique identifier field | Unlike XC Bank's mock (which exposes a clean `id` per transaction by design), this real production page's visible transaction rows have no single unique id — the composite of every visible field is the best available proxy, consistent with the general "verify against what's actually observable" discipline already applied elsewhere this session. |
| SCB transaction-detail expansion — idempotent, checks before clicking | `check-transactions.ts` checks whether "Transaction Description" (a field only visible when a row's "▾" chevron is expanded) is already showing before clicking that chevron | Found empirically: the chevron **toggles** open/closed. A first version always clicked it, which worked once but then *re-collapsed* the row on a second same-session check, silently losing the detail. Fixed by checking current state first, matching the same "verify actual behavior, don't assume" discipline used for the window-maximize/chevron-toggle findings elsewhere in this project. |
| SCB monitor notifications — first-check baseline vs. per-transaction detail | First-ever check sends a balance-only "monitoring started" message; every check after that sends full per-transaction detail (direction, amount, description, expanded Channel/Cheque No./Teller No./Branch Code fields, updated balance) only for genuinely *new* transactions | Matches the user's two explicit, separate requests: notify with the real starting balance on first entry, and notify with full in/out/amount/detail/updated-balance for anything that happens afterward — deliberately not the same message shape for both, since dumping full detail for every already-existing historical transaction on first setup would be spam, not a notification. |
| SCB monitor — separate BullMQ scheduler, mirrors XC Bank's exactly | `queue.ts` gained a second, fully independent scheduler (`monitor:scb-business-anywhere-1`, its own job types, its own `SCB_MONITOR_INTERVAL_MS`/`SCB_MONITOR_JITTER_MS`, its own auto-stop) rather than generalizing the existing XC Bank scheduler to be multi-site | Same reasoning already applied to the XC Bank monitor's own pause/autostop job types staying separate from each other ("avoid touching that already-tested mechanism") — a real-money, real-bank scheduler is exactly the wrong place to introduce risk by refactoring a working, already-verified mechanism (XC Bank's) to make room for a second use case. Default interval (5 min) and jitter (15s) both deliberately longer than XC Bank's dev-speed defaults, since this hits a real production site. |
| SCB monitor — every notification includes both balances; mismatch gets a warning symbol | `formatBalanceLine()` in `scb-monitor.ts` appends `Avail: X \| Ledger: Y THB` to every Telegram message (not just transaction ones), and prepends a `⚠️` warning line whenever the two figures differ, mirrored in the live page's UI as a dedicated warning banner | Explicit request: Available vs Ledger mismatch is a real signal (usually an incoming transfer recorded but not yet clearable/usable), not noise — worth surfacing on every check, not only when a transaction is also detected, so a mismatch is visible even on an otherwise-quiet poll. |
| SCB monitor — scheduled loop started for real, bounded (60 min auto-stop), not unlimited | First real scheduled run of this monitor used `autoStopMinutes: 60` rather than an unbounded run, even though the user gave a full go-ahead ("คุณทำได้เลย") | Matches the standing caution from the prior entry ("consider a short auto-stop-bounded test before trusting it fully unattended") — the user's own go-ahead is about building/starting the feature, not a request to skip the safety default this project has applied to every other real-site loop; a bounded first run is the reversible choice, extending afterward is easy if needed. |
| SCB monitor — session-expired fast detection + one-time alert, found from a real drop | The scheduled loop's session genuinely expired for real during this session — the check just hung for the default ~30s timeout with an unclear generic error instead of failing fast. Fixed in `check-transactions.ts`: checks for the login username field (short 3s timeout) and for "Account Summary"'s absence (5s timeout) *before* attempting anything else, throwing a `SESSION_EXPIRED:`-prefixed error immediately. `scb-monitor.ts` recognizes that prefix and sends a distinct "please log in again via noVNC" Telegram alert exactly once per expiry episode (a new `sessionExpiredNotified` state flag, reset the moment a check succeeds again) rather than either staying silent or re-alerting every single failed retry | Real production sessions do expire — this needed to be handled cleanly, not assumed away. The bot still never attempts to log back in itself, matching every other credential-handling decision in this log; it only detects and alerts. |
| SCB — re-login resets the active company selection, found from a real re-login | After the real session-expiry above, the user logged back in manually, and the very next check reported a different account entirely — Available: 0 / Ledger: 4,082.09 THB (a real mismatch, but for **"บริษัท 2 ยู เอสเตท จำกัด"**, the account's default, not "เซซุส" which had been actively monitored). Confirmed by re-checking: the company switcher had silently reset to the default company on fresh login — "active company" is apparently not preserved across a full re-authentication, only within an already-live session (matches the earlier, separate finding that a `page.goto()` reload also loses it — same underlying client/session-state fact, now confirmed true for a full re-login too) | **Operationally important, not yet automated**: after *any* re-login (whether from an expired session or a fresh one), the correct company must be re-selected via `POST .../select-company` before the monitor's data can be trusted again — the monitor itself has no concept of "which company it's supposed to be watching" and will silently report whatever's currently active. Recovered manually this round (re-selected "เซซุส", confirmed the real pending transfer — see below — showed up correctly afterward). A future improvement could have the monitor remember its intended company and re-assert it after every check, but that wasn't built this round — flagging it here so it isn't silently assumed to already work. |
| SCB monitor — first real incoming transfer, detected and notified successfully | A real +831.68 THB incoming transfer ("รับโอนจาก BAY x3539 MAXBIT DIGITA") was found by the very next check after switching back to the correct company — captured correctly (amount, description, expanded Channel/Teller/Branch detail), deduped correctly (`seenTransactionKeys` grew from 1 to 2, not re-notified on a repeat check), and the Available/Ledger mismatch from the pending-clearance period was gone by the time this check ran (both settled to 8,209.30 THB) | End-to-end proof with genuine, unprompted real-world money movement (not a manufactured test) that the whole pipeline — company context, extraction, dedup, mismatch detection, Telegram delivery — works together correctly, not just in isolated pieces. |
| Telegram group support added alongside private chat, same bot token | `telegram.ts`'s `sendTelegramMessage()` now fans out to both `TELEGRAM_CHAT_ID_XC` (private, unchanged) and a new `TELEGRAM_GROUP_CHAT_ID` (independent, both optional) — no call site elsewhere needed to change. Group chat id (`-1003924358603`, "Small and snoopy") found live via the real bot's own `getUpdates` response after the user added it to the group and sent `/start`, not guessed from the id's shape alone | Explicit request: reuse the same working bot token (no new bot), add group delivery without removing private delivery. Verified for real: a single `sendTelegramMessage()` call delivered to both destinations with no error logged. |
| SCB timezone display bug — fixed at the Docker/container level, not per-script | Real transaction times shown on the live SCB page (and thus in Telegram reports) didn't match actual Thailand time. Root cause: the lane's browser container had no explicit timezone (Debian's own UTC default), and SCB's own UI explicitly formats displayed dates using the browser's *local* timezone (its own disclaimer banner says so). Fixed by adding `tzdata` to `services/browser-worker/Dockerfile` and setting `TZ=Asia/Bangkok` in `docker-compose.yml`'s `browser-worker-scb-business-anywhere-1` service — not a per-script CDP `Emulation.setTimezoneOverride` call (an earlier draft of this fix, discarded) | Explicit user direction: fix at the Docker level, not the script level. A container-level fix covers every script/workflow/manual noVNC session uniformly (including a human's own manual browsing), where a per-script override only helps scripts that remember to call it — matches the same "fix the actual root cause, not each symptom" discipline used throughout this project. Verified live: `date` inside the recreated container shows the correct `+07` offset, and a fresh check afterward showed the same real transactions' dates correctly rolled over a day later with the right time (`08/08/2026 02:55` instead of the earlier wrong `07/08/2026 19:55`). |
| SCB live page — editable script/loop reference, `localStorage`-only | New "📜 Script & loop reference" section on `scb-business-anywhere-live.html`, a `<textarea>` pre-filled with what each of the four lane scripts does and the scheduled loop's exact start/end boundaries (outer schedule vs. one tick's inner steps), editable in place with a "Reset to default" button. Saved via `localStorage` in the user's own browser only — no new backend route, nothing sent to the server | Explicit request for an in-page, editable reference — chose `localStorage` over adding a persistence API since these are personal operational notes, not shared/authoritative state; keeps the change purely client-side (no new attack surface, no new data ever leaving the browser). |
| SCB monitor — sticky target company, auto-reasserted every check | New `services/worker/src/company-switcher.ts` (`selectCompany()`, shared by `select-company.ts` and `check-transactions.ts` — also fixed a real fragility in the dropdown-opener along the way: it used to open the dropdown by clicking any text containing "บริษัท", which only works when *that specific* company happens to be the active header, not "เซซุส"/"กฤษฎิ์ ดำประสงค์"; now tries each of the 3 known company names to find whichever is currently showing). `scb-monitor.ts` state gained `targetCompany` (set automatically whenever "Switch company" succeeds via `server.ts`); `check-transactions.ts` re-asserts it (base64-encoded, same argv-encoding reasoning) at the start of *every single check*, unconditionally — a harmless no-op/re-click if already active | Explicit request, and closes the real gap flagged two entries ago (re-login resets the active company to the account default, silently making the monitor report the wrong one). Verified live: deliberately force-switched the browser to "2 U Estate" outside the API (simulating an external reset) with `targetCompany` still saved as "เซซุส", then ran a normal check — it correctly re-switched to เซซุส first and reported its real balance (8,209.30 THB), not 2 U Estate's. |
| SCB company switcher — a real 30s hang found and fixed via `selectCompany()`, twice | First finding: clicking the header when it's *already* showing the target company doesn't no-op — it's the same toggle button that opens the dropdown, so the click actually opens the menu, and that menu's own popover overlay then blocks the very click's own actionability retries, hanging the full 30s. Second finding, right after fixing the first: if a *previous* run left the dropdown open (e.g. it hung/was interrupted mid-selection — exactly what the first bug had just caused), every subsequent run inherits that already-open state, and clicking the already-highlighted target item doesn't reliably close the menu either (same overlay-blocks-itself symptom). Both fixed together: `selectCompany()` now presses `Escape` unconditionally first (confirmed directly that this closes a real stuck-open dropdown; harmless no-op otherwise) to guarantee a known closed state before deciding whether anything else needs clicking | Neither bug was hypothesized in advance — both were found by actually running the new auto-reassert feature against the real site and reading the real Playwright timeout traces (which named the exact blocking element, `.MuiPopover-root`), not guessed at. Recorded in detail since the second bug is a direct, non-obvious consequence of the first (a hang leaves recoverable-but-unusual UI state behind) — worth knowing if a similar "click something that might already be selected" pattern comes up elsewhere in this project. |
| SCB monitor — screenshot on every check, success or failure | `check-transactions.ts` now takes a screenshot at the end of a successful check *and* wraps the whole check in a try/catch that screenshots on **any** failure (not just the `SESSION_EXPIRED` cases), embedding the filename in the thrown error message (`"... (screenshot: <filename>)"`) since that's the only channel available to carry data out of a thrown error back to `scb-monitor.ts`. New `latestScreenshot` state field, displayed on the live page | Explicit request for a passive visual trail across the monitor's own already-running ~5-min ticks (to catch a suspected idle-timeout popup in the act) rather than a human actively polling the noVNC screen every 5 minutes themselves. Verified live: a genuine real session expiry (English-language login page this time, not Thai) was correctly captured in a screenshot and surfaced via `latestScreenshot`. |
| SCB "View Details" click occasionally silently skipped — a bare `isVisible()` isn't a wait | Found live, right after login: the "All Accounts" card list (where "View Details" lives) loads asynchronously further down the page, and a bare `isVisible()` call (an instant one-shot check, not a poll/retry) sometimes fired before it rendered, silently leaving the check stuck on the summary overview with zero transactions extracted — no error, just wrong/empty data. Fixed with `isVisible({ timeout: 6000 })`, which polls | A quiet, no-error failure mode is worse than a loud one — this one would have kept reporting "0 transactions" indefinitely without ever surfacing as `lastError`, easy to miss. Verified live: a check that had returned an empty array now correctly returns both real transactions again. |
| Telegram incoming commands (`/status`, `/screenshot`, `/help`) — explicit tiny allowlist, routed through the same queue | `telegram.ts` gained `getTelegramUpdates()` (polling, not a webhook — no public endpoint exists for this loopback-bound dev stack) and `sendTelegramPhoto()`. New `telegram-commands.ts`: a `setInterval` poll loop (offset persisted to `data/telegram-command-offset.json` so a restart doesn't reprocess old messages) that only acts on messages from the already-configured private chat or group (`isKnownTelegramChat()` — anything else silently ignored, not just unrecognized-command-ignored) and only recognizes an explicit 4-command map. `/screenshot`/`/status` enqueue new job types (`SCB_TELEGRAM_SCREENSHOT_JOB_NAME`/`SCB_TELEGRAM_STATUS_JOB_NAME`) through the *same* BullMQ queue as the scheduled monitor checks — never run directly from the poll loop — so a Telegram-triggered action can never execute concurrently with a scheduled check on the shared browser. `/help`/`/start` just send static text directly, no queue/browser involvement | Explicit request for "override" capability — inspecting/commanding the bot mid-operation. Scoped deliberately narrow: both real commands are strictly read-only (screenshot reuses `analyze-page.ts`, which never navigates/clicks/types; status only reads already-saved state) — this is a text-driven trigger channel, and per the standing credential-handling boundary (see multiple earlier decision-log rows), it must never grow into something that types/clicks/navigates a real form from arbitrary Telegram text. Verified: both job handlers enqueued and processed cleanly by the already-running worker with no Telegram-send errors logged; the actual incoming-message detection (a real user sending a command) still needs to be confirmed live by the user. |
| "Hover to inspect" request — explained as not buildable as literally asked, given a real architectural constraint | User asked whether hovering the mouse in noVNC and asking the bot "what's this" could work. Explained directly: noVNC's mouse input goes through the VNC protocol straight to X11/Chromium's OS-level input, entirely separate from the CDP/Playwright channel the bot uses — the bot has no visibility into the human's live cursor position, full stop, not a missing feature. Offered `/screenshot` (already built) as the practical alternative: human looks at the image, asks a question in the conversation directly | Being explicit about a hard technical constraint (not just choosing not to build it) matters here — a workaround that *looked* like it solved this without addressing the actual channel separation would have been misleading. No code changes resulted from this specific ask; the `/screenshot` command already satisfies the closest achievable version of it. |
| "Auto-resume after manual override" — already true by design, not a new feature | User asked for the bot to automatically know what to do after a manual override ends. Explained rather than built: every scheduled/manual check (`check-transactions.ts`) *already* independently re-verifies session state, re-asserts the target company, and re-navigates to Account Summary from scratch on every single run, regardless of whatever state a human left the page in between ticks — so the next tick (at most ~5 min later) self-corrects with no extra state-tracking needed | Recognizing "this is already true" and explaining why, instead of building a redundant explicit override/resume state machine, avoids adding complexity for a property the architecture already has as a side effect of the session-expiry and sticky-target-company work from earlier this session. |
| SCB screenshots switched to `fullPage: true` — immediately caught the real idle-timeout dialog | All four screenshot call sites updated. The very next captured failure showed the real dialog for the first time: **"For your online security, you have been logged out of SCB Business Anywhere — Please log in again."**, a single "OK" button, no "stay logged in"/keep-alive option | This resolves the open question from two entries ago (whether the suspected idle popup was dismissible to prevent the drop): it isn't — by the time it appears, the session is *already* terminated server-side, "OK" only acknowledges. Confirms the existing SESSION_EXPIRED detection + one-time Telegram alert is the correct and only viable handling; no "dismiss the popup to stay logged in" logic should be built, there's nothing to dismiss into staying logged in. |
| Queue worker process died silently mid-session — noted, root cause not found | The host `npm run worker` process (holds no port, found via `Get-CimInstance Win32_Process | Where CommandLine -like '*worker.ts*'`) was confirmed gone with **zero error output** in its own log — several manually-triggered check-once jobs sat unprocessed in the queue (never failed, never completed) until the process was restarted and picked up the backlog | Flagged rather than silently worked around, since a silently-dead queue consumer is a real reliability gap for a monitor meant to run unattended — matches this project's own established discipline (e.g. the earlier Chromium-crash blind spot) of surfacing gaps like this explicitly rather than only fixing the immediate symptom. No root cause identified this round (no crash log, no OOM signal checked); worth adding process-liveness monitoring/auto-restart if this recurs. |
| SCB balance/transactions widget doesn't auto-refresh — bot was silently reading a stale cached snapshot | The widget shows "Last Updated: <date>, <time>  Refresh" and only pulls current data when that "Refresh" link is clicked — it does **not** poll on its own. `check-transactions.ts` never clicked it, so every check (scheduled or manual) kept re-reading whatever snapshot was on-page as of the last time a *human* happened to refresh it, silently missing any transaction that posted after that. Fixed: `runCheck()` now clicks "Refresh" (if visible, `isVisible({timeout:3000})`) right after the "Latest Transactions" heading appears, waits 2.5s for the re-fetch, then extracts. Verified live: before the fix, `latestTransactions`/balance were stuck reporting an old snapshot; the very next check after adding the click surfaced a real, previously-invisible -6,000 THB transaction and the Available/Ledger balance dropped from 8,209.30 to 2,209.30 accordingly, which correctly fired the "new transaction" Telegram alert | User noticed the "Last Updated" timestamp didn't match reality ("เพราะ last update ไม่ตรงกับข้อมูลจริง") and asked directly to test clicking Refresh — this is why "there's a transaction but no automatic notification" was happening: the notification logic itself was correct, but the data feeding it was stale. No amount of check-interval tuning would have fixed this; the missing click was the actual gap. |
| SCB check interval shortened 5min→~70-105s (randomized), plus a "did Refresh actually work" self-check | `SCB_MONITOR_INTERVAL_MS`/`SCB_MONITOR_JITTER_MS` defaults changed 300_000/15_000 → 87_500/17_500, so real requests land 70-105s apart, randomized per tick (not a fixed cadence). Also added `pageLastUpdatedText`/`staleRefreshStreak`/`staleRefreshNotified` to monitor state: the page's own "Last Updated" text is captured every check and compared to the prior check's value — since ticks are now under 2 minutes apart, it should advance almost every time if Refresh genuinely pulled fresh data; 3+ consecutive unchanged readings triggers a one-shot Telegram warning that Refresh may be silently failing even though the check itself reports no error. Verified live: real consecutive ticks landed 87s apart, `pageLastUpdatedText` advanced correctly (`15:44`→`15:45`) with the streak counter resetting as designed | Explicit request for faster detection plus randomized timing specifically to avoid a perfectly regular request pattern (user's own words: "อย่าให้เป๊ะ จะได้ไม่โดนจับได้"). Told the user directly this is a real, only partially-mitigated tradeoff: jitter defeats a naive fixed-interval check, it does not make continuous 24/7 polling look like human usage to a more sophisticated fraud-detection system — if the account is ever flagged/rate-limited, this interval is the first thing to widen back out. The stale-refresh self-check exists because the *previous* incident (transaction happened, no alert) was a silent failure with no error surfaced — this closes the same class of gap for "Refresh click stopped working" specifically. |
| Record → analyze → run (element + pixel + keyboard), incl. real SCB lane — a deliberate capability expansion beyond read-only | New `clickSmart`/`pressKey`/`typeText` action types in `registry.ts` (element-first, pixel-fallback click; ordinary text fields only). New `record-actions.ts`: observes the already-open page (never navigates), refuses outright while the SCB login page is showing, injects a single in-page listener function (passed directly to `page.evaluate()`, not via `.toString()`+`eval()` — see the separate finding below for why) that redacts any password-type/`autocomplete=*password*` field's real keystrokes *before* they ever leave the browser, replacing them with a fixed sentinel (`REDACTED_FIELD_SENTINEL`, exported from `registry.ts` so both sides share the exact same value); `typeText` throws `REDACTED_FIELD:` outright if it ever sees that sentinel, rather than silently skipping or typing a placeholder. Stops via a polled stop-flag file (host-writable directly, no docker call needed) or a hard 15-min max. Compiled result reviewed in a new Recorder UI section before saving. Saved scripts run through `runWorkflowOnLane()` (generalized from the existing `runWorkflow()`) against the SCB lane's own worker/recordings dir, and can be triggered manually, on a schedule (own BullMQ scheduler per script name), or via a new Telegram `/run <name>` | User explicitly requested this, including for the real SCB lane, confirmed via AskUserQuestion (element-based-primary/pixel-fallback recording; SCB lane included; scripts triggerable manually+scheduled+Telegram, not manual-only). This is the first capability on this lane beyond strictly read-only — the credential-redaction mechanism above is the one non-negotiable carried over unchanged from the standing boundary (see `webop-credential-boundary` in Claude's own memory); everything else about *what* a script can click is the risky-keyword gate below, a separate, weaker guarantee. |
| Risky-keyword Telegram confirm gate for SCB script replay — best-effort, not a guarantee | New `scb-replay.ts`: runs a saved script segment-by-segment (not as one `run-workflow.ts` process) — every step whose `selector`/`text` params match a case-insensitive keyword list (`transfer, pay, payment, confirm, submit, send, bill payment, payroll, delete, remove, โอนเงิน, ชำระ, จ่าย, ยืนยัน, ส่ง, ลบ`) pauses the run, writes `pendingConfirmation` to a lane-scoped state file, sends a Telegram message naming the exact step, and polls that state file (not Telegram directly — `telegram-commands.ts`'s own poll loop is the single consumer of `getTelegramUpdates()`'s offset, to avoid two pollers racing the same offset) for a `/confirm`/`/cancel` reply, timing out (treated as cancel) after 10 minutes. Everything else in a script runs with no gate at all | Explicit user decision after being asked directly: not a hard block on risky actions (unlike the credential redaction, which *is* hard) — a live human-in-the-loop check instead, so the same script can safely contain both "look at a report" steps (no friction) and "click Confirm on a transfer" steps (always needs a real-time yes). Explicitly caveated to the user and recorded here: a real button whose text doesn't happen to match one of these keywords (different wording, a different language) isn't caught — this is the one thing standing between a replayed script and a real transaction, so it's documented as best-effort, not "safe by construction." |
| `page.evaluate()` + manually stringified/eval'd functions breaks under tsx/esbuild's `__name()` wrapping | First version of `record-actions.ts` defined its in-page listener setup as ordinary top-level TS functions, then shipped them into the browser via `fn.toString()` + `eval()` inside a wrapping `page.evaluate()` call — failed live with `ReferenceError: __name is not defined` inside the eval'd code. Root cause: tsx transpiles via esbuild, which wraps named function declarations in a `__name(fn, "fn")` helper call (to preserve `.name`); the wrapper reference doesn't exist in the isolated context a manually re-eval'd function body runs in. Fixed by nesting all the in-page logic inside a *single* function passed directly to `page.evaluate(installRecorderInPage)` — Playwright's own function-serialization path handles this correctly, only the manual toString()+eval() indirection was broken | Found live against the real SCB lane (`install-recorder` step failing), not assumed or caught by `tsc --noEmit` (both are valid TypeScript — this is a runtime transpilation-output detail invisible to the type checker). Worth remembering project-wide: never ship a TS function into a browser context via `.toString()` + `eval()` — pass it directly to `page.evaluate()`/`page.addInitScript()` instead, every time. |
| **Correction to the row above**: passing the function directly to `page.evaluate()` was NOT the full fix — `__name` wrapping applies to any function *expression* assigned to a local `const`/`let`, not just named declarations | Built `services/scb-mock/` (see two rows down) specifically to safely re-test record→analyze→run without the real account, and the `__name is not defined` error recurred immediately even with the "fixed" direct-`page.evaluate()` version. Root cause isolated empirically (compiled a minimal repro with `npx tsx`, printed `fn.toString()` directly): esbuild wraps `const computeSelector = (el) => {...}` into `const computeSelector = __name(el => {...}, "computeSelector")` — since `installRecorderInPage` declares several such local consts (`computeSelector`/`isCredentialField`/`flush`), their wrapping calls land *inside its own body*, which is exactly the text Playwright ships into the page. Confirmed the actual boundary directly: a function assigned to a **property** (`w.__name = (fn) => fn`) or passed as a **bare inline argument** does NOT get wrapped, only local variable/const bindings do. Real, permanent fix: a `installRecorderWithShim()` helper that first `page.evaluate()`s a tiny `window.__name = window.__name \|\| ((fn) => fn)` passthrough (itself written as a property assignment, not a local const, so it can't have the same problem), before evaluating `installRecorderInPage` — fixes the whole class of issue permanently rather than constraining every future in-page function to avoid local-const patterns | Recorded here specifically so a future session doesn't re-trust the earlier row's "fixed" conclusion at face value — it looked fixed (worked once against the real lane) but wasn't the real root cause, only fixed by coincidence for that one function shape. The shim is copy-pasteable to any other file that ships a function into a browser context via `page.evaluate()`. |
| Built `services/scb-mock/` — an isolated mock of SCB Business Anywhere, same posture as `services/xc-bank` | New Express service, DOM text/structure deliberately close enough to the real site that `check-transactions.ts`/`select-company.ts`/`record-actions.ts` run against it **completely unchanged** (verified, not assumed) — same balance/transaction regex extraction, same company-switcher text-based selection (reuses the exact 3 real company display names already hardcoded in `company-switcher.ts`'s `KNOWN_COMPANIES`, since those are just display names, not secrets), same "Last Updated: ... Refresh" stale-until-clicked behavior the earlier real-account bug fix targets. Session-timeout overlay (`POST /dev/session-timeout-overlay`, or a real "Simulate session timeout (dev)" button in the UI) renders a real `position:fixed` dialog on top of the still-authenticated page — same mechanism (not a simulation of the mechanism) as the real site's own blocking overlay, cleared only by a real "OK" click (`POST /dev/acknowledge-timeout`), no auto-dismiss. New `/transfer` page (mock-only — a POST there can never move real money, full stop) with a From/To/Amount/Memo form → confirmation step → mock success page, specifically so record→analyze→run and the risky-keyword confirm gate have something realistic and *safe* to record/replay against. Navigating the real SCB lane's browser to the mock uses a new `scb-business-anywhere-mock-open-login.json` workflow (name deliberately prefixed `scb-business-anywhere` so it stays excluded from the shared/generic workflow list, same existing guard) | User directed this explicitly, prioritizing it over further real-account testing: "เอาข้อมูลเก่าก่อนหน้านี้แล้วมาใช้งานในการ dev" (use what we already captured from the real site as reference for building this). This unblocked verifying the *entire* record→analyze→run pipeline (redaction, element+pixel click, multi-field typing, the risky-keyword confirm gate, all three) end-to-end for the first time this session — the real-account version had only gotten as far as "the recorder installs and produces an empty result," never a real captured-and-replayed action, because further testing there was correctly blocked (see the earlier classifier-block entry). |
| Recorder bugs found via mock testing, both real and generalizable, not mock-specific | (1) `computeSelector()`'s text-based selector used Playwright's `text=${text}` (unquoted = case-insensitive **substring** match) — found live that clicking a "Transfers" sub-link this produces a selector that *also* substring-matches an unrelated "Payments and Transfers" parent link elsewhere on the same page, and `clickSmart`'s `.first()` picked the wrong (no-op) one on replay. Same trap independently re-discovered in an `<h1>Transfer</h1>` heading vs. the "Transfer" submit button. Fixed: selectors now use quoted `text="${text}"` (exact match). (2) The recorder's `pending` typed-field tracker only ever held ONE in-flight field — moving between form fields via a real fill (any real typing flow, not just `.fill()`) without an intervening click/Tab/Enter overwrote the previous field's pending state before it was ever flushed, silently dropping every typed field except the last one. Fixed: the `input` listener now flushes any pending value for a *different* element before starting to track the new one | Both found by actually recording+replaying a real multi-field, multi-click flow (the mock's Transfer form) end-to-end, not by code review — the first several attempts produced visibly wrong or incomplete compiled scripts, which is what surfaced these. General lesson, not scoped to this one page: any future in-page selector-computation or event-tracking logic should assume ambiguous substring matches and multi-field sequences are the norm, not the exception. |
| `run-workflow.ts`'s own page-reset step broke cross-segment continuity in `scb-replay.ts` | `scb-replay.ts` runs a saved recording **segment by segment** (splitting at each risky-keyword step) — each segment is its own `docker compose run` invocation through the generic `run-workflow.ts` engine. That engine's `prepare-page` step closes every existing page and opens a blank one before *every single run*, by design, to prevent tab buildup across a long-running monitor loop. Found live: a replay's 2nd+ segment always landed on a fresh blank page, losing the login/navigation/filled-fields state the previous segment had just built up — manifesting first as `browserContext.newPage: Protocol error (Target.createTarget): Failed to open a new tab` (a real Chromium-level symptom of the underlying design conflict, not just a cosmetic one), then as `locator('#toAccount')` timeouts once the immediate crash was worked around. Fixed with a new opt-in `KEEP_EXISTING_PAGE=1` env var on `run-workflow.ts`'s `prepare-page` step (reuses `context.pages()[0]`, same pattern `analyze-page.ts`/`check-transactions.ts` already use, instead of closing+reopening) — set only by `runScbRecording()` in `exec.ts`, the one and only caller `scb-replay.ts` uses; every other caller (the shared Workflows list, the XC Bank monitor, the `scb-business-anywhere-mock-*` test workflows) is completely unaffected, default behavior unchanged | This is the kind of bug that only surfaces when two features built independently (the monitor-loop-oriented page-reset from an earlier round, and this round's segment-by-segment replay) are actually run together for the first time — neither one was wrong in isolation. Verified the fix directly: confirmed live that a full 6-step recording (nav click → 3 field fills → 2 risky-keyword-gated clicks, 3 separate `/confirm` pauses) replayed correctly end-to-end only after this fix, landing on the real mock "Mock Transfer Submitted" success page. |
| Full record→analyze→run + risky-keyword confirm gate, verified end-to-end for the first time this session, against the mock | Recorded a real click-through of the mock's Transfer flow (nav click → fill To Account/Amount/Memo → submit → confirm), saved it, ran it via the API. It correctly paused **three separate times** (the "Transfers" nav click, the Transfer submit button, the Confirm Transfer button — all three match `DANGEROUS_KEYWORDS` since "transfer"/"confirm" appear in their selectors), sent a real Telegram message each time, and correctly resumed on a simulated `/confirm` and correctly aborted on a simulated `/cancel` (tested both paths). Final run: all 6 steps completed, landing on "Mock Transfer Submitted — no real funds were moved." | This is the first genuine, complete, verified proof that the whole pipeline (redaction → compile → save → segment-by-segment replay → risky-keyword pause → Telegram → resume/abort) works correctly together, not just each piece in isolation — directly addresses what the earlier real-account testing round couldn't reach (it only got as far as confirming the recorder *installs* against the real lane). |
| Recurring environment instability observed this round, not fully root-caused | Both the SCB lane's browser container (`browserContext.newPage: Protocol error`, `CDP endpoint never became ready`) and the control-panel `worker.ts` queue consumer (silently dying mid-session, no error logged — the same open gap noted in an earlier decision-log row) needed manual restarts multiple times during this round's heavy back-to-back `docker compose run` testing. A `docker compose restart` sometimes wasn't enough (container kept cycling `Shutting down...` → full X11/Chromium/noVNC restart in a loop); a clean `stop` + `up -d` reliably recovered it. No root cause confirmed — plausibly connection/resource pressure from the sheer volume of rapid CDP connect/disconnect cycles this testing round generated, but not verified against real metrics | Flagged rather than silently worked around, matching this project's established practice for reliability gaps. Practical mitigation used this round (not a fix): after any `docker compose restart`, explicitly verify CDP responds (`fetch('http://localhost:9222/json/version')`) before proceeding, rather than assuming a few seconds' wait was enough — saved real time versus repeatedly hitting the same failure. Worth real process-liveness monitoring/auto-restart if this keeps recurring in normal (non-testing-flood) usage too. |
| **Root cause found** for most of the above instability: Xvfb's stale `/tmp/.X99-lock` never cleared on container restart | A crashed or soft-restarted (`docker compose stop`+`up`, which reuses the same container filesystem/tmpfs) Xvfb instance left `/tmp/.X99-lock` behind. On the next start, Xvfb refused to launch at all (`Fatal server error: Server is already active for display 99`) while the rest of `entrypoint.sh` (Fluxbox/Chromium/x11vnc/noVNC) launched anyway against a display that was never actually created — CDP then never became reachable, with the real cause invisible in the symptoms (`Failed to open a new tab`, `CDP endpoint never became ready`). Fixed in `services/browser-worker/entrypoint.sh`: `rm -f "/tmp/.X${DISPLAY_NUM}-lock" "/tmp/.X11-unix/X${DISPLAY_NUM}"` right before starting Xvfb — mirrors the exact same cleanup already applied a few lines below for Chromium's own profile lock | This explains most of the "recurring environment instability" logged in the row above, which was flagged but not root-caused at the time. Also fixed, same round: Fluxbox's default `fbsetbg` wallpaper-setter failed with no `feh`/Eterm installed and popped a blocking `xmessage` dialog over noVNC (cosmetic only — never affected CDP/automation) — fixed by shipping a minimal custom `/root/.fluxbox/startup` (`exec fluxbox`, no wallpaper step) via the Dockerfile rather than installing an unneeded package for a container with no real use for a wallpaper. |
| `.userpassmock` — host-editable, gitignored file to pre-fill the mock's login form | New `D:\WebOperator\.userpassmock` (gitignored, distinct from the pre-existing `.userpass`, which the bot never reads automatically for any real site), read fresh on every page render by `services/scb-mock/src/server.ts`'s `readUserPassMock()`. Displayed as small gray hint text *below* the login form (`.hint`-styled `<p>`), not pre-filled into the input `value=` attributes — the input boxes stay visually empty/clean to match the real SCB site's own look, per explicit request | Convenience for repeated mock testing (no retyping placeholder creds every session) without ever touching the real-credential boundary — safe specifically because the mock never validates against these values, any non-empty string works. The user later put a real-looking username/password pair into this file themselves, a deliberate and informed choice distinct from the standing refusal to write such values anywhere else (see `webop-credential-boundary` in Claude's own memory) — this file's entire purpose is mock-only and gitignored. |
| Mock login/password pages redesigned to visually match the real SCB two-column layout | `loginPage()`/`passwordPage()` in `services/scb-mock/src/server.ts` rebuilt around a shared `authLeftPanel()` helper (logo, announcement box, illustration) alongside the form column, based on a real screenshot captured earlier this session | Purely cosmetic fidelity request — makes the mock a closer stand-in for visual/UX review, no behavioral change to any selector `record-actions.ts`/`check-transactions.ts` depend on (verified those still run unchanged). |
| **Record → analyze → run generalized from SCB-only to any lane** — the credential-boundary/redaction guarantees carry over unchanged, only the *scope* changed | User explicitly rejected a per-page/per-site implementation of recording ("ต้องทำให้ universal คือได้กับทุก web นะไม่เว้น... ไม่ใช่เราไปแก้หน้านั้นๆ แต่เราต้องทำให้อัดได้") after being asked whether to test record→analyze→run against the mock or the real account next. Full refactor: new `services/control-panel/src/lanes.ts` lane registry (`"shared"` — the existing `browser-worker-chrome`/`worker` pair already used by XC Bank/the-internet/demo/scb-mock — and `"scb-business-anywhere-1"`, the isolated real-bank lane); every recording/replay function in `exec.ts`/`queue.ts`/`server.ts` takes a `laneId`; routes became `/api/lanes/:laneId/recordings/*`, always 404ing on an unknown lane rather than falling back to `shared`; `scb-replay.ts` renamed to `replay-engine.ts`, `runRecordingReplay(laneId, name)`; BullMQ scheduler ids became `recording-schedule:${laneId}:${name}`; replay-state moved to a dotfile colocated per-lane (`.replay-state.json`, auto-excluded by the existing "not starting with a dot" recordings filter). The credential-page guard (`record-actions.ts`'s `guard-not-login-page` step) was simultaneously made **structural instead of SCB-text-specific**: was a literal check for the Thai string `"ชื่อผู้ใช้งาน"` (SCB's own username label, would never match any other site), now checks for any visible field shaped like a credential — `input[type="password"]`, or `autocomplete`/`id`/`name` containing one of `password/otp/pin/token/secret` (`CREDENTIAL_HINT_KEYWORDS`, shared with the in-page per-field redaction so both checks always agree). New reusable frontend component `services/control-panel/public/recorder-ui.js`, mounted via `<div id="recorder-root" data-lane-base="/api/lanes/<laneId>">` — used unchanged by both `/` (new Recorder section, explicit warning that the shared browser is dev/test-only) and the SCB live page (replacing ~230 lines of page-specific inline JS) | This is a scope/architecture change, not a new capability — everything the credential-boundary memory and the risky-keyword confirm gate already guaranteed for the SCB lane is now guaranteed identically for any lane, nothing was loosened to make this generalization possible. The user's detailed approval message added several explicit requirements folded directly into the implementation rather than left as follow-ups: the guard is explicitly documented as **best-effort, not a guarantee** (a genuinely non-standard credential widget with none of the hint keywords would not be caught); every route fails 404 on an unknown `laneId`, never silently defaulting to `shared`; Telegram `/run <name>` searches every lane and refuses (naming the lanes) rather than picking one if a name collides across lanes; `recorder-ui.js` must work on both pages with zero copied logic. |
| Lane-generalization verification, done end-to-end post-refactor | (1) Universal guard: recording refused on the **shared** lane (not SCB) while a password field was visible on scb-mock's `/password` page, with the new broadened message. (2) Full record→save→run cycle on the shared lane: recorded a single click ("Refresh" on scb-mock's account-summary page), saved, ran the saved script back through `/api/lanes/shared/recordings/*` — completed correctly. (3) SCB-lane regression: re-ran the pre-existing `test-mock-transfer` recording through the renamed/generalized `/api/lanes/scb-business-anywhere-1/recordings/test-mock-transfer/run` route — identical behavior to before the refactor (3 separate risky-keyword pauses, all three resolved via `resolvePendingConfirmation()`, all 6 steps completed). (4) UI parity: `recorder-ui.js` screenshotted rendering correctly and identically on both `/` and `/monitors/scb-business-anywhere/live`. (5) Telegram `/run`: exercised the real `handleRun()` (temporarily exported, reverted after) for the no-arg/unknown-name/cross-lane-ambiguity cases — all three real Telegram messages sent with no send errors logged | `tsc --noEmit` stayed clean throughout the refactor but, per this project's own established pattern, that alone doesn't prove behavioral correctness — every plan verification step was actually run against live containers, not just typechecked. Also incidentally re-confirmed the Xvfb stale-lock fix above: `browser-worker-chrome` hit the exact `Failed to open a new tab`/`CDP endpoint never became ready` symptom once mid-verification from an unrelated stuck-page state, recovered cleanly via `stop`+`up -d`, consistent with (not a regression of) the earlier fix — the fix addresses the *stale lock on restart* case specifically, not every possible way a page/tab can wedge. |

*9 generic action types now live in `services/worker/src/actions/registry.ts`: the original 6 (`navigate`/`dismissPopup`/`login`/`extract`/`saveSession`/`screenshot`) plus `xcBankLogin`/`xcBankExtractDashboard`/`xcBankLogoutClean`. `dismissPopup` carries forward a real lesson from `adapters/the-internet.ts`: the-internet's ad modal appears via `setTimeout(showAd, 500)`, not on initial render, so the handler waits for visibility rather than checking it immediately.*

## Phase 1 — Prototype

- [x] Docker เปิด Chromium/Firefox แบบเห็นหน้าจอ (`services/browser-worker`)
- [x] เชื่อม noVNC (`http://localhost:6080` / `:6081`)
- [x] ช่องทาง handoff ระหว่าง Codex/Claude/human (`docs/AGENT_HANDOFF.md`)
- [x] Control Panel มี Start/Stop/Take control — `services/control-panel`, `npm start` → `http://localhost:4000`, verified: start/stop both browsers, embedded noVNC "take control", all four worker actions run through the UI
- [ ] เปิดเว็บ ทดลอง login ด้วยมือ
- [x] บันทึกและนำ browser session กลับมาใช้ (Playwright `storageState`) — `services/worker` `npm run save`/`npm run restore`, verified round-trip via synthetic marker
- [x] ทำ adapter เว็บตัวอย่างหนึ่งเว็บ — `services/worker/src/adapters/the-internet.ts` + `npm run adapter`, verified: dismisses popup, real login, extracts flash message, saves real session
- [x] Playwright worker เชื่อมต่อเข้า browser ผ่าน CDP (Chromium) — `services/worker`, verified: navigates, reads title, screenshots
- [x] Playwright worker เชื่อมต่อ Firefox — via Playwright's own `launchServer()`/`connect()` protocol, not literally WebDriver BiDi (see decision log). `services/worker/src/firefoxConnect.ts` + `run-firefox-demo.ts`, new `worker-firefox` compose service. Verified end-to-end through direct CLI and the real Control Panel queue. Scope: connection-proof only (one demo script), not full adapter/workflow parity yet. Profile persistence and cross-job page visibility are *not* preserved for Firefox — see decision log.

## Phase 2 — Task Engine

- [x] Queue และ scheduler (Redis + BullMQ) — `services/control-panel` `src/queue.ts`, Control Panel UI enqueues the 4 worker actions instead of running them synchronously; verified: async return, sequential execution (concurrency 1), retry config wired (`attempts: 2`). Job API/UI now also surfaces start time + duration (`processedOn`/`durationMs`) and exit code, not just stdout/stderr/steps. The queue consumer now runs as its own process (`npm run worker`), separate from the API/UI (`npm start`) — verified a job survives the API process being killed mid-run, and a job enqueued while the worker is down correctly waits rather than being lost.
- [x] Step-based workflow — `services/worker/src/run-workflow.ts` + `src/actions/registry.ts` + `workflows/the-internet-login.json`: a job now runs a *sequence of generic, parameterized actions* defined as data, not one hardcoded script. Control Panel: `GET /api/workflows`, `POST /api/enqueue-workflow/:name`. Verified end-to-end through the real queue, including the `extract` step's scraped text surfacing in the job detail UI. Workflows are now validated in full (every step's action `type` checked against the registry) *before* connecting to any browser, so a malformed workflow fails instantly with zero side effects instead of partially executing — verified directly (a bad `type` fails on a `validate` step with no `connect` line ever logged) and through the real Control Panel (a genuinely-broken-JSON file 400s at enqueue time, before a job even exists). **The workflow engine is now the primary orchestration path**: the "Run demo" and "Run example adapter" Control Panel buttons enqueue workflows (`demo.json` — new; `the-internet-login.json` — already existed) instead of the original 4 fixed actions, which are down to 2 (`save`/`restore`) — neither migrates onto today's action registry without adding new action types (bespoke marker-setting JS, mid-workflow context-swapping), so they're intentionally left as fixed actions rather than forced. Underlying fixed npm scripts (`npm run start`/`npm run adapter`) remain directly runnable via CLI even though the Panel no longer routes to them as enqueueable actions.
- [x] screenshot/trace ทุกจุดสำคัญ — partial: `services/worker/src/steps.ts` `step()` wrapper reports name/status/detail/screenshot per stage in all 4 worker scripts, parsed by `services/control-panel/src/exec.ts` and shown in an expandable job row (`/screenshots/*` static route). Full DOM/console/network trace capture still open.
- [x] retry (partial) — whole-job retry already existed; now also **per-step retry** in the workflow engine (`services/worker/src/steps.ts` `stepWithRetry`), configurable via an optional `retry: {attempts, delayMs}` field per step, with safe defaults (`navigate`/`dismissPopup`/`extract`/`screenshot` retry automatically, `login`/`saveSession` require explicit opt-in). Verified: a genuinely-flaky step recovers and reports which attempt succeeded; an always-failing step exhausts its attempts and reports the count; a clean first-try success shows no attempt noise. No circuit breaker yet, no timeout-per-step beyond Playwright's own action timeouts.
- [x] ดาวน์โหลดและจัดเก็บข้อมูล (MinIO/S3) — partial: `services/worker/src/artifacts.ts` (`uploadArtifact`) mirrors every screenshot *and* every saved session (fixed scripts + the generic `screenshot`/`saveSession` workflow actions) to a `minio` compose service (`weboperator-artifacts` bucket, `screenshots/*` and `sessions/*` prefixes), added alongside the existing local files, not replacing them. Best-effort via `stepBestEffort()` — an upload failure shows as an `archive-screenshot`/`archive-session` step error but never fails the job. The Control Panel now also reads screenshots *back* from MinIO: `GET /api/artifacts/screenshots/:filename` (`services/control-panel/src/artifacts.ts` + `server.ts`) streams the object through Express, and the job step detail UI shows a "MinIO" link next to the existing local "screenshot" link whenever that job's archive step succeeded. Verified end-to-end: worker→MinIO connectivity through Docker service DNS, genuine upload+fetch-back round trips with matching SHA-256 hashes (including through the new Control Panel route, not just the worker), non-fatal behavior with MinIO stopped, and archive/read paths all showing up correctly through the real Control Panel queue with the existing `/screenshots/*` route unaffected. Session files remain plaintext dev-only in MinIO too — no encryption added, and no session-reading route was added (no session UI exists to begin with). The artifact read route is now generalized to `GET /api/artifacts/:kind/:filename` with an explicit allowlist (`screenshots`, `downloads`, `videos`, `traces` — `sessions` deliberately excluded, enforced structurally not just by omission). Downloads has a real, empirically-confirmed blocker (`connectOverCDP` to an externally-launched, separate-container Chromium can't retrieve download artifact bytes via Playwright's own APIs — see decision log for root cause and the real fix, which needs a shared volume + raw CDP + file-watching, deliberately not built this round) and video/trace remain a design note only (both are context-creation-time Playwright options, no concrete workflow needs them yet). MinIO-backed session retrieval is still not done — no session-reading route exists, by design.

## Phase 3 — Gmail

- [x] Google OAuth — partial/scaffold: `services/worker/src/gmail/client.ts` (config from `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`GOOGLE_REDIRECT_URI` env vars only, throws a clear error if any are missing), `authorize.ts` (CLI: prints the consent URL for the human to open in their own browser — never automated — then a one-shot local HTTP listener captures the redirect and exchanges the code for tokens). Least-privilege `gmail.readonly` scope, `access_type: offline` + `prompt: consent` for a reliable refresh token. Verified: TypeScript build clean, readable no-env-var and no-token failure paths (no real credentials needed), auth URL shape correct (fake-but-well-formed credentials), the local callback listener's parse/respond/exchange/error-handling path exercised for real against a fake code (Google correctly rejected it with `invalid_client`, caught cleanly). **No live OAuth flow with a real Google Cloud project/account has been done** — no real credentials exist in this environment, and one wasn't attempted without asking first, per the explicit instruction.
- [ ] อ่าน ค้นหา และดาวน์โหลดไฟล์แนบผ่าน Gmail API — partial: `services/worker/src/gmail/list-messages.ts` proves a minimal `users.messages.list` read (count + IDs only, not content) once a token exists — not yet tested against a real account. Search, message bodies/attachments, and download are not implemented yet.
- [ ] Browser fallback เฉพาะกรณีจำเป็น — not started; this round deliberately added zero browser automation of Gmail, per the explicit instruction.
- [ ] Encrypted token vault — not started by design this round; current token storage is an explicitly-documented plaintext dev-only placeholder (`data/gmail-tokens/gmail-token.json`, gitignored), not safe for a real account.

## Phase 4 — Universal Adapters

- [ ] ระบบ plugin สำหรับเพิ่มเว็บไซต์
- [ ] Popup rules กลาง
- [ ] page-state detection
- [ ] selector หลายระดับ: role → label → text → CSS
- [ ] workflow versioning เพื่อย้อนกลับเมื่อเว็บเปลี่ยน

## Phase 5 — Production

- [x] แยก worker หลายเครื่อง (partial) — the queue consumer is now a separate *process* from the Control Panel API (`services/control-panel` `npm run worker`); still runs on the same host today, not literally distributed across machines yet, but the architectural split (independent restart/crash, independent scaling) is done.
- [ ] Chrome และ Firefox profiles (multi-user)
- [ ] สิทธิ์ผู้ใช้และ audit log
- [ ] domain allowlist
- [ ] monitoring และแจ้งเตือน
- [ ] backup/restore session vault
- [ ] Chromium sandbox hardening (drop `--no-sandbox`, apply seccomp profile per Playwright Docker docs)
- [ ] noVNC behind HTTPS with short-lived, per-session URLs (currently plain HTTP + static password — dev only)

## Test Fixtures

- [x] XC Bank — `services/xc-bank`, an isolated mock third-party bank site for practicing browser automation against, in the same relationship as `the-internet.herokuapp.com` except self-hosted. Login flow now has three paths, matching real bank UX: fresh (no session, fills username + password), remembered-username (a pending session skips straight to `/password`), already-authenticated (skips both). Test account `demo_user`/`demo_pass` (mock only, documented on the page itself), transaction dashboard with deterministic-per-session-and-time-window data (changes automatically over time, forceable via a dev-only regenerate endpoint). `Logout`/`Logout clean` buttons on the site itself (`Logout` keeps the remembered username; `Logout clean` is a **dev/test-only** full reset, not real-bank behavior) plus a matching `xcBankLogoutClean` worker action/workflow for resetting test state programmatically. Strictly isolated from WebOperator — no shared code/DB/queue/artifact store, HTTP/browser only, verified structurally via grep, not just by convention. Adapter: `services/worker/src/adapters/xc-bank.ts` + workflows `xc-bank-login-extract.json`/`xc-bank-logout-clean.json`, reusing the existing workflow engine/MinIO archival/Control Panel Workflows UI unchanged. Design-only scaffolding for a future email-notification feature (see decision log) — not implemented, not Phase 3 work.
- [x] XC Bank Monitor — a continuous check loop (`services/control-panel/src/monitor.ts`) plus a new, separate Control Panel page (`/monitors/xc-bank`) distinct from the existing noVNC take-control page (untouched). Reuses the *existing* `xcBankLogin`/`xcBankExtractDashboard` actions unchanged — no new worker-side login/extraction logic, just a dedicated `xc-bank-monitor-check.json` workflow and a small, generically useful `${ENV_VAR}` substitution in `run-workflow.ts` so each check gets a distinct screenshot filename. New-transaction dedup keyed on XC Bank's own transaction reference/id (a `Set`-backed filter — architecturally impossible to double-notify a seen ref), chronological order, the transaction table itself unduped (always latest state). Screenshot timeline capped at 200, oldest pruned both locally and from MinIO (best-effort). Runs through the *existing* BullMQ queue/concurrency-1 Worker via a BullMQ Job Scheduler (`upsertJobScheduler`/`removeJobScheduler`) — no second locking mechanism, can never race a manual job on the same shared browser. Dev-only JSON state at `data/monitor-state/xc-bank.json` — no real credentials, only the mock test creds already public on the XC Bank page. See decision log for the full verification story, including a real bug found and fixed during testing. **Follow-up round**: the page shipped with a relative-script-path bug that meant it never actually rendered anything dynamic in a real browser — found, fixed, and re-verified by running the page's own JS against real data. `/` is now a proper Control Center with a data-driven monitor registry (`services/control-panel/src/monitors-registry.ts` + `GET /api/monitors`) so a future second monitor appears automatically; the screenshot timeline now shows real image thumbnails, not just text links. **Second follow-up round**: the monitor now has two views — `/monitors/xc-bank` (history/detail, unchanged) and new `/monitors/xc-bank/live` (live/current-operation view: noVNC iframe of the actual shared browser on the left with a Chrome-not-running/latest-screenshot fallback, a polling extracted-data panel on the right) — no new API routes, both cross-linked, `/`'s Monitors section now links to both. See decision log.

## Immediate next step

**Phase 1 is now functionally complete.** Phase 2 has a queue (now running
as an independently-restartable process), per-step status/screenshots with
retry, a generic multi-action workflow engine now serving as the primary
orchestration path (2 of the original 4 fixed actions migrated onto it;
`save`/`restore` intentionally stay fixed — see decision log), a
generalized MinIO/S3 artifact system (screenshots + sessions archived,
readable back through a kind-parameterized Control Panel route). **Phase
3 (Gmail) has started**: an OAuth/Gmail API dev scaffold
(`services/worker/src/gmail/*`) — auth URL generation, local callback
token exchange, a minimal read-only list proof — with zero browser
automation of Gmail and a plaintext dev-only token placeholder, explicitly
not the real encrypted vault yet. **A new isolated test fixture, XC
Bank**, is also in place (`services/xc-bank` + its adapter/workflow) —
a self-hosted mock third-party site for practicing browser automation
without depending on a real external site staying stable, with
design-only scaffolding for a future email-notification feature (see
decision log; nothing email-related is built yet) and login/logout flows
matching real bank UX. **XC Bank Monitor** builds on top of it: a
continuous check loop (BullMQ Job Scheduler, same queue/concurrency-1
model as everything else) with new-transaction dedup, a 200-screenshot
retention timeline, and a dedicated `/monitors/xc-bank` Control Panel
page — the existing noVNC take-control page untouched. That page's
initial ship had a real bug (relative script path meant it never
rendered anything dynamic) — found and fixed in a follow-up round, which
also turned `/` into a proper Control Center with a data-driven monitor
registry (`GET /api/monitors`) so future monitors appear automatically,
and upgraded the screenshot timeline to real image thumbnails. A second
follow-up round split the monitor into two views: the original page
stays history/detail, and a new `/monitors/xc-bank/live` shows a live
two-column operation view (the real shared browser via the existing
noVNC endpoint on the left, a polling extracted-data panel on the right,
with a Chrome-not-running fallback) — both linked from `/` and from each
other. The workflow engine also gained a **"polite automation" pass**
(`services/worker/src/policy.ts` + `challenge.ts`): a per-site
locale/timezone/pacing policy (applied via raw CDP, since the engine
reuses an existing browser context rather than creating a fresh one),
randomized action/typing delay, and a challenge-page detector that
stops rather than bypasses — explicitly scoped to false-positive
reduction, not fingerprint evasion (no `navigator.webdriver` patching,
no CAPTCHA solving, no proxy/IP work — all discussed and deliberately
excluded). The monitor's own scheduling interval got a matching jitter
in `queue.ts`. **Monitor Control UX** followed on top: Pause (lighter
than Stop — keeps the scheduler running, just skips scheduled ticks)
alongside Stop, bulk Pause-all/Stop-all across the monitor registry, a
next-check-time estimate and a long-running/screenshot-count warning
(both derived from existing state, no new tracking), a scheduled-vs-
manual badge on monitor jobs in the Jobs table, and a dev-only Cleanup
button. Found and fixed a real concurrency bug while verifying: pause/
resume were first implemented as direct file writes and got silently
clobbered by an in-flight `checkOnce()`'s own stale save — fixed by
routing them through the same concurrency-1 queue, same as cleanup
already was. **Health/diagnostics** followed, an explicit direction
change away from continuing Phase 3 (paused, not blocked): a read-only
`services/control-panel/src/health.ts` module + `GET /api/health` +
`/health` page cover every dependency (4 Docker services, the API
itself, queue worker connectivity via `getWorkers()`, Redis via a
bounded-timeout check, MinIO, XC Bank's URL, noVNC) — never starts or
stops anything itself, only shows the exact fix command as text. The
Control Center gained a System Health banner + "Run readiness check" +
a Jobs-section warning when the queue worker is disconnected. Found and
fixed the same AggregateError-empty-`.message` issue (already fixed
once for the artifact route) independently in the new MinIO health
check. Verified against two real induced failures (MinIO stopped, queue
worker stopped), not just the healthy path. Still open: a
live test against a real Google Cloud OAuth client/account (needs the
user's own credentials and explicit go-ahead), search/message-content/
attachment reading, the encrypted token vault, the real downloads fix
(shared volume + raw CDP + file-watching, deliberately deferred — see
decision log), video/trace (needs a concrete workflow use case first),
turning the XC Bank Monitor's design-only email-notification fields into
something real, and — if it ever becomes worth the effort — a way to
keep a Firefox page alive across worker connections (would need a small
always-connected keep-alive client; not pursued now).
